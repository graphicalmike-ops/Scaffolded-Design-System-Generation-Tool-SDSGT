{
  "name": "sdsgt-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "SDSGT pipeline CLI core (seed input -> promote -> generate). Local development only — not published.",
  "scripts": {
    "promote": "node src/cli.ts promote",
    "generate": "node src/cli.ts generate",
    "typecheck": "tsc --noEmit",
    "qa:matrix": "node scripts/qa-matrix.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@material/material-color-utilities": "^0.3.0",
    "style-dictionary": "^5.5.3"
  }
}
