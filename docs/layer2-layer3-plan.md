# Layer 2 (component vendoring) + Layer 3 (scaffolding) — execution plan

Written 2026-09-11, for execution in a fresh session with no memory of the conversation that produced it. This is a **session-crossing execution plan**, not itself a contract — it's the "what to do next and why" handoff. Any real decision made while executing it (a version pin, a naming choice, a scope cut) should still get written back into `pipeline-plan.md`/`contracts-and-seeds.md`, same as every other decision in this project. This file can go stale; those two can't be allowed to.

## Where things actually stand (verified 2026-09-10/11, re-check before trusting)

- **Layer 1 (tokens)** — done. `promote` + `generate` cover all 5 base platforms (Tailwind v4, Bootstrap 5.3, MD2/MUI 9.x, MD3/Compose 1.4.0, SwiftUI), Figma token push works against a free-plan file.
- **Layer 2, theme-variable mapping** — done, for all 7 component libraries on the original list: `generate --shadcn` / `--rnr` / `--rn-paper` / `--vuetify`, plus shadcn-vue/React-Bootstrap/bootstrap-vue-next reusing `--shadcn`'s/`--bootstrap`'s output as-is. See `contracts-and-seeds.md`, "shadcn/ui theming" through "Vuetify theming."
- **Layer 2, Pattern A (npm-dependency libraries) — done (2026-09-11).** `--rn-paper`/`--vuetify` each also write a `SETUP.md` next to their `theme.ts`: npm install command(s) plus a real, ready-to-copy provider-wiring snippet (`PaperProvider`/`createVuetify()`) that imports the actual theme export names, branched on which of light/dark exist. Both confirmed to have no init CLI that can target an existing project, so this is instructions + a snippet, not something the pipeline runs itself — see Subject 2 below for the verification detail. QA matrix (`checks.ts`) now asserts `SETUP.md` exists and imports match `theme.ts`'s real exports.
- **Layer 2, Pattern B — shadcn/ui done (2026-09-12), RNR done (2026-09-14).** `scaffold --framework nextjs --shadcn` runs shadcn's own CLI for real (`init -d -y` then `add --all -y`) — every current shadcn/ui component (61 at build time) vendored into a real Next.js project, already styled with real brand colors, plus a `sdsgt-vendored-components.json` hash manifest guarding against a future re-vendor silently overwriting a customized file. RNR's own `init` really is fully interactive with no non-interactive flags (confirmed by actually running it) — that finding was correct and still holds. What reversed the overall "blocked" conclusion: `add` (which already had real non-interactive flags) turns out to run standalone without ever needing `init`, once a real `components.json` exists and the git working tree is clean — both real, satisfiable preconditions. `scaffold --framework react-native --rnr` now vendors real RNR components the same way `--shadcn` does for Next.js/Vue.js. Full detail in Subject 2 of this doc.
- **Layer 3 (project scaffolding) — Next.js + Tailwind done (2026-09-12, Bootstrap/React-Bootstrap path added later), Vue.js done (2026-09-14, all three design-language paths — Bootstrap added later the same day), React Native (Expo) done (2026-09-14), Kotlin AND SwiftUI both resolved as guides instead of automation (2026-09-14).** `node src/cli.ts scaffold --framework nextjs [--shadcn]` drives `create-next-app` (and, with `--shadcn`, shadcn's own CLI too) non-interactively and layers this pipeline's generated tokens/fonts/agent-rules on top — see Subject 3 below for the full detail, including a real Tailwind dark-mode bug this work found and fixed in already-shipped Layer 1/2 code, and `... --framework nextjs --bootstrap` (React-Bootstrap, via `create-next-app --no-tailwind` plus the same real Sass build pattern as Vue.js's own Bootstrap path). `node src/cli.ts scaffold --framework vuejs [--shadcn]` (Tailwind, via `create-vue`), `... --framework vuejs --vuetify` (Material Design, via `create-vuetify` — a genuinely separate pipeline, not a variant), and `... --framework vuejs --bootstrap` (bootstrap-vue-next, via `create-vue` again plus a real Sass build wiring in this pipeline's generated `_variables.scss` before Bootstrap's own Sass) are all now built — see Subject 4 below for the real bugs/gotchas found along the way. `node src/cli.ts scaffold --framework react-native` (NativeWind, via `create-expo-app` + `generate --rnr`'s theme — NOT `--tailwind`, which is Tailwind-v4-shaped and incompatible with NativeWind v4's real Tailwind-v3 peer dependency) and `... --framework react-native --rn-paper` (MD3, via a real `react-native-paper` install) are both built too — see Subject 5 below, including the NativeWind/Tailwind-v3 finding that shaped the whole thing. **Neither Kotlin nor SwiftUI has a `scaffold` command at all — real research (not assumed, for both, independently) found no official non-interactive project-creation CLI exists for either Android/Compose or Xcode/SwiftUI, a structurally different situation from every framework above.** `generate --md3`/`--swiftui` each always write a real, filled-in `SCAFFOLD_GUIDE.md` instead — see Subjects 6 and 7 below. **Layer 3 now has a real answer — automated scaffold or real guide — for every one of the five frameworks this pipeline supports.**
- **The `SDSGT-start` skill's `componentLibrary` gap is fixed** — see Subject 1 (done 2026-09-11). Skill also now drives the real scaffold step (step 10 in "Running the flow") for Next.js+Tailwind, Vue.js+Tailwind, Vue.js+Material Design, Vue.js+Bootstrap, React Native+Tailwind, and React Native+Material Design (React Native done 2026-09-14, same session as Subject 5; Vue.js+Bootstrap added later the same day, see Subject 4), and surfaces Kotlin's and SwiftUI's own `SCAFFOLD_GUIDE.md` files at step 9 instead of a step 10 gate for either, since there's no scaffold command to run for them (both done 2026-09-14, same session as Subjects 6–7).

**Don't trust version/API specifics in this doc without re-checking.** This project got burned mid-session by exactly this: shadcn shipped a new CLI major, Vuetify moved to a new major with the old line going LTS, and React Native Paper turned out to now default to MD3 instead of MD2 — all things that would've been wrong if assumed from training data instead of checked against real, current source. Whoever executes this plan should re-verify anything here that a library's own docs/source can confirm or deny, the same way every prior session in this project did.

---

## Subject 1 — Fix the stale `SDSGT-start` skill (do this first)

**Problem:** `.claude/skills/SDSGT-start/SKILL.md` still tells the user the component-library seed question "has no effect on today's output" and that Layer 2 theming "is not started." That's no longer true, but the skill was never updated to actually call the new `generate` flags. Right now `--shadcn`/`--rnr`/`--rn-paper`/`--vuetify` are CLI-only — invisible to anyone going through the normal conversational flow, which is the actual product surface for this tool.

**Concrete gap, verified:** `cli/src/types/seed-config.ts`'s `SeedConfig` interface has **no `componentLibrary` field at all** — not even a placeholder optional field the way `figmaFileUrl`/`figmaMcp` exist ahead of the official-MCP path being built. The skill asks the question but the answer is never persisted anywhere machine-readable.

**What needs to happen:**
1. Add a real `componentLibrary` field to `SeedConfig` (optional, since not every framework/design-language combo has one yet — Kotlin/SwiftUI's "library" is really just the platform itself).
2. Update the skill's row 4 (component library question) to stop saying "no effect" — instead, map the answer to the right `generate` flag(s) based on `targetFramework` + `targetDesignLanguage`, per the table already in the skill (`SKILL.md` lines ~89-90) and in `contracts-and-seeds.md`'s "Seed inputs" table: e.g. Next.js + Tailwind + "shadcn/ui" → `generate --shadcn`; React Native + Tailwind + "RNR" → `generate --rnr`; React Native + MD3 + "React Native Paper" → `generate --rn-paper`; Vue.js + MD3/MD2 + "Vuetify" → `generate --vuetify`. Bootstrap-family and MUI/MD2 targets already resolve to existing `--bootstrap`/`--md2` flags with no new work.
3. Update `SKILL.md`'s "Running the flow" step that actually invokes `generate`, so it passes the right flag(s) through.
4. Sweep the rest of the skill for other now-stale "Layer 2 not started" language (there may be more than one spot — grep for "Layer 2" and "not started" across `SKILL.md`).

This is small and has no real design decisions left in it — the mapping table already exists in two places (`SKILL.md` itself, `contracts-and-seeds.md`), it just needs to be wired through. Good first task in the new session, low risk, unblocks everything else being reachable by an actual user.

---

## Subject 2 — Layer 2: actually vendoring components

This splits into two genuinely different problems — confirmed by checking how each library actually distributes its components, not assumed:

### Pattern A — npm-dependency libraries (React Native Paper, Vuetify) — **done, 2026-09-11**

No vendoring at all. "Installing" means: add the npm package, wrap the app in the library's theme provider, pass in the `theme.ts` this pipeline already generates. No curated component list to choose (the whole library ships as one package), no customized-file-marking problem (nothing gets copied into the user's repo to diverge from upstream).

**Verified, not assumed (2026-09-11):** neither library has an init command that can wire a provider into an *existing* project. React Native Paper has no scaffolding/init CLI at all (`oss.callstack.com/react-native-paper` — always a manual `PaperProvider` wrap). Vuetify's `npm create vuetify`/`create-vuetify` only scaffolds a brand-new project (confirmed against Vuetify's own `packages/docs/.../installation.md`, master branch) — no mode targets an existing one. So the answer to "run something vs. emit a snippet" is settled: emit a snippet, for both.

**Built:** `generate --rn-paper` and `generate --vuetify` each now also write a `SETUP.md` next to `theme.ts` — real npm install command(s), RN Paper's optional `babel.config.js` bundle-size plugin, and a ready-to-copy provider-wiring snippet (`PaperProvider`/`createVuetify()`) that imports `theme.ts`'s *actual* export names (`LightTheme`/`DarkTheme` or `lightTheme`/`darkTheme`), branched on whichever of light/dark really exist — not a hardcoded both-modes assumption. See `contracts-and-seeds.md`, "React Native Paper theming"/"Vuetify theming," for the written-up decision. QA coverage added in `scripts/qa-matrix/checks.ts` (`checkRnPaper`/`checkVuetify`): asserts `SETUP.md` exists and that its imports/`defaultTheme` match what `theme.ts` actually exports for that seed's light/dark mode — full matrix run clean (35/35 cases, 5111 checks, 0 anomalies). No real Layer 3 infrastructure was needed for this, confirming the original guess above.

### Pattern B — copy-paste libraries (shadcn/ui done 2026-09-12, RNR done 2026-09-14)

Real vendoring. Installing means running the library's own CLI against a **real, already-initialized project** — `shadcn add` specifically needs `components.json` already present, which only exists after `shadcn init` has run inside a real Next.js+Tailwind project. This is what actually coupled Layer 2's hard half to Layer 3: there was no way to vendor a real shadcn component without a real scaffolded project to vendor it into — which is why this waited for the Next.js scaffold slice above.

**shadcn/ui built** (`cli/src/scaffold/nextjs.ts`, `scaffold --framework nextjs --shadcn`) — the three open decisions below are now resolved, each confirmed against a real end-to-end run (real build succeeded with 61 real vendored components + this pipeline's own brand colors/fonts applied):

- **Curated/trimmed component list — decided: everything.** `shadcn add --all -y` vendors the entire current registry (61 components at time of building, confirmed via a real run) rather than a curated subset. Chosen over a curated list specifically so the user never has to come back and run `shadcn add <name>` by hand for something this pipeline could have already vendored — matches `pipeline-plan.md`, "Who this is for, and the bar for 'done.'" RNR needs this same decision made independently when its turn comes (its own catalog, not identical to shadcn's).
- **Customized-file marking (the ACIM lesson) — decided: a manifest with content hashes.** `sdsgt-vendored-components.json`, written at the project root, records each vendored file's path + a SHA-256 hash of its original content (`{ "shadcn": { "button": { "path": "...", "hash": "..." }, ... } }`). A future re-vendor step (not yet built) or an AI agent can compare a file's current hash against the manifest: unchanged means safe to re-vendor, changed means customized — leave it alone. `generate/project-docs.ts`'s `AGENTS.md` rule for shadcn now names this file explicitly, closing the gap the rule used to only gesture at. Nothing reads the manifest back yet — writing it is this session's scope; the re-vendor step that would consume it is a separate, not-yet-built piece.
- **Theme wiring order — decided: append, don't suppress or replace.** Real testing (`shadcn init -d -y`, current CLI v4.21.0) confirmed there's no flag to suppress its default theme generation — it always writes a real, structurally-necessary `globals.css` (imports, dark-mode custom-variant, the generic `@theme inline` mapping that real vendored components' Tailwind utility classes depend on) with its own default OKLCH values, and even vendors one Button component by default. Rather than parsing and replacing that structure, this pipeline's own `generate --shadcn` output (already using shadcn's exact CSS variable names, e.g. `--primary`, `--card`) gets **appended** to the end of the file. CSS's own cascade — a later declaration of the same custom property under the same selector wins — makes this correct without ever touching shadcn's generated structure, confirmed by inspecting the real compiled output through a real build: two "dead" earlier `:root`/`.dark` blocks (shadcn's own defaults, superseded) followed by one live one (this pipeline's real values). Font roles (`--font-sans`/`--font-heading`, shadcn's only two) get this project's secondary/primary fonts respectively, same append strategy.

Also confirmed and worth remembering: `shadcn add --all` re-running against `init`'s already-vendored Button correctly detected an identical file and skipped it rather than erroring or duplicating — no extra handling needed for that overlap.

**RNR — blocked, real reasons, 2026-09-12 (decided: don't build around it, don't script keystrokes).** Investigated for real (ran the actual CLI, not docs/training-data assumptions) before concluding this — the situation turned out meaningfully different from shadcn's, not just "the same pattern for a second library":

- **RNR has its own separate CLI** (`@react-native-reusables/cli`; not a shadcn registry despite the shared design philosophy). **Correction, 2026-09-14:** the `1.0.0` real-current-version claim recorded here on 2026-09-12 was itself wrong — real npm version history tops out at `0.7.1` (confirmed via `npm view @react-native-reusables/cli versions`). The confusion is explainable, not just an error: the CLI's own `--help`/`--version` output prints `@react-native-reusables/cli 1.0.0` as a hardcoded self-reported string that doesn't match its published `package.json` version at all — a real, verified discrepancy between what the tool says about itself and what npm actually has, worth remembering if this ever needs re-checking again. `add` looks fully automatable on paper: `-y`/`--yes`, `-a`/`--all`, `--styling-library nativewind|uniwind`, mirroring shadcn's `add` closely.
- **`init`, however, is a fully interactive wizard with no flag to skip any of it** — confirmed by actually running it against both an empty directory and an already-scaffolded Expo project: asks for a project name (no positional argument or flag found), asks you to pick a template via an arrow-key menu (Minimal NativeWind / Minimal Uniwind / Clerk auth NativeWind), asks "install dependencies?" (Y/n), then asks which package manager — **defaults to `bun`, not npm** — and when the default was accepted on this machine (no `bun` installed), the install step failed outright. None of `init`'s documented flags (`-c`/`--cwd`, `-t`/`--template`) cover any of this.
- **A real chicken-and-egg problem shadcn's slice never had:** `add` needs NativeWind already configured in the target project, which normally comes from `init`. Unlike shadcn (which had the already-built Next.js scaffold to install into), **this pipeline has no Expo/React Native scaffold at all yet** — so even setting `init` aside, there's no existing "Layer 3 for React Native" to hand `add` a project.

**Decided: stop here, don't script around it.** The alternative — piping the exact expected keystroke sequence (typed project name, arrow-key menu selections, "y") into `init`'s stdin — would work today but is fragile by construction: it isn't reading a documented contract, it's mimicking a human typing, and breaks silently the moment RNR reorders or rewords a prompt. That's a real conflict with this project's own "determinism is a project-wide goal" principle (`pipeline-plan.md`, "Three layers"), in a way every other scaffolding integration in this pipeline (`create-next-app`, `shadcn`) never had to trade off, since both of those ship real non-interactive flags.

**Re-verified 2026-09-14, still blocked at the time — the Expo scaffold now exists (Subject 5, below), but the CLI blocker was real and independent.** With a real `create-expo-app`-scaffolded project now on disk (something that didn't exist on 2026-09-12), `@react-native-reusables/cli@latest init -t default` was run again, non-interactively, stdin closed. It hangs — confirmed via `--log-level trace` that it's genuinely alive and waiting, and via a bounded background run that it's sitting on a live interactive menu: `"A project already exists in this directory. How would you like to proceed?"` with three arrow-key options. No flag bypasses this specific prompt. So the chicken-and-egg half of the original blocker (no Expo scaffold to install into) was resolved, but `init`'s own interactivity was not — **that conclusion about `init` specifically was correct and still stands.**

**REVERSED the same day, once `add` was tested standalone instead of via `init` — real vendoring works.** The user asked, after Kotlin/SwiftUI both got resolved as guides, whether the same treatment could apply to RNR. It can't (RNR's whole point is vendoring real component *source*, not a project-creation gap a guide can solve) — but that question prompted re-testing whether `add` (which already had real non-interactive flags: `-a`/`--all`, `--styling-library`) could run standalone against a project that never went through `init` at all, something flagged as "not yet tested" back on 2026-09-12 and still true as of the morning's Subject 5 re-check. It can. Run against this pipeline's own `scaffold --framework react-native` output:
- `add` correctly auto-detected NativeWind as the styling library by inspecting the real project — no `init`-derived state needed for that part at all.
- It then asked exactly two things, both real, satisfiable preconditions: (1) a plain y/n prompt — "Missing components.json — write one?" — which never fires if the file already exists (real schema fetched from RNR's own template repo, not guessed); (2) an arrow-key prompt — "Git repository is dirty — continue anyway?" (confirmed via a real bounded test that plain piped text does NOT satisfy it, same raw-keypress class as `init`'s own blocked menu) — which never fires if the working tree is actually clean (a real `git add -A && git commit`, since `create-expo-app` already initializes a real git repo).
- With both satisfied, `add -a --styling-library nativewind` completed for real: exit 0, 32 real component files, real `@rn-primitives/*` dependencies installed. One transient connect-timeout to `reactnativereusables.com` was hit once during testing and resolved on retry — same class of flakiness as create-vuetify's own template download (Subject 4), unrelated to the interactivity question.
- **Two more real gaps found by then typechecking the real output** (same discipline that caught shadcn-vue's own `@unovis` gap): `add --all` never vendors `lib/utils.ts` (the `cn()` helper almost every component imports — normally `init`'s job) — fixed by writing it directly, real content fetched from the same template repo. And `add --all` doesn't install every real dependency its own vendored files import — `react-native-screens`, `lucide-react-native`, `class-variance-authority`, `clsx`, `tailwind-merge` were all missing — fixed with explicit follow-up installs. A real `tsc --noEmit` and a real `expo export --platform web` both pass clean against the final output.

**Built as `--rnr` on `scaffold --framework react-native`** (`cli/src/scaffold/react-native.ts`), same customization-guard manifest treatment (`sdsgt-vendored-components.json`) as shadcn/shadcn-vue. See `contracts-and-seeds.md`, "React Native (Expo) scaffold (Layer 3)," for the full write-up. The lesson worth keeping: the original "don't script keystrokes" decision was correct and still applies to `init` — what changed the outcome was finding the tool has a *second*, less-interactive entry point (`add`) that doesn't need the blocked one at all, not reversing the decision itself.

---

## Subject 3 — Layer 3: project scaffolding

**Next.js + Tailwind slice built, 2026-09-12.** `cli/src/scaffold/nextjs.ts` (`node src/cli.ts scaffold --framework nextjs`) drives `create-next-app` non-interactively (verified: a full explicit flag set + `--yes` runs with zero prompts even with stdin closed), then layers this pipeline's already-generated Tailwind theme on top — colors, fonts, and agent-rules files all wired in before the user ever opens the project. Concretely, per run:
- Runs `create-next-app` with a locked flag set (TS, Tailwind, ESLint, App Router, `src/` dir, npm, git, `--no-agents-md` to suppress Next's own generic `AGENTS.md`/`CLAUDE.md` in favor of SDSGT's own).
- Copies in `theme.css`/`theme-light.css`/`theme-dark.css` from the matching `generate --tailwind --framework nextjs` run, and rewrites `globals.css` to import them plus set `body`'s background/text/font from the semantic tokens.
- Real fonts: deliberately does **not** use `next/font/google` (that API needs a static import matching Google's own current export identifier for the family — spaces to underscores — which risks drifting from whatever the user actually typed, since Google has renamed families before, e.g. "Source Sans Pro" → "Source Sans 3"). Instead reuses the exact same already-fetched `.woff2` files `promote --fonts-dir` already consumes (`shared/font-slug.ts`) and writes a plain hand-authored `@font-face` block using the literal family name already baked into every semantic typography token — no identifier-guessing, verified end-to-end with real fetched font files (build succeeded, font file served correctly at runtime).
- Strips Next's own placeholder homepage/Geist-font wiring, writes a minimal real page proving the tokens are live, and its own `README.md`/`AGENTS.md`/`foundations-rules.md`/`design.md` (copied from the `generate` run).
- Scope deliberately narrow, matching "the smallest possible Layer 3 slice" below: Next.js + Tailwind only. Bootstrap/MUI Next.js targets, and every other framework (Vue.js, React Native/Expo, Kotlin, SwiftUI), still have no scaffold builder.

**A real bug found and fixed in the process (2026-09-12):** the first real Next.js build of this pipeline's Tailwind output exposed that `generate/tailwind.ts`'s dark-mode file nested `@theme { ... }` inside `[data-theme="dark"] { ... }` — Tailwind v4 hoists any `@theme` block straight to the root regardless of what selector it's nested in, so dark values were silently overwriting light ones globally, all the time, not just under that selector. The QA matrix's existing checks never caught this because they only verified the generated file's raw text (does the selector string appear, are braces balanced), never what a real Tailwind compiler actually does with it. Fixed by using plain (non-`@theme`) custom properties under the selector instead, which behave the way an ordinary CSS custom property is expected to. A new check, `checkTailwindRealBuild` (`scripts/qa-matrix/checks.ts`), now actually compiles the generated CSS through the real `@tailwindcss/postcss` plugin and inspects the output — added `tailwindcss`/`@tailwindcss/postcss`/`postcss` as `cli/` devDependencies for this. Verified the check has real teeth by reintroducing the bug and confirming it fails with a clear message, then confirming it passes again once reverted. Full matrix: 35/35 cases, 5173 checks, 0 anomalies.

**Framework order, settled:** Next.js went first, per the recommendation below — Vue.js, React Native/Expo, Kotlin/Android Gradle, and Xcode/iOS remain unstarted. Given Subject 2's coupling, Next.js also sets up shadcn/ui as the natural next real end-to-end proof (Layer 1 tokens → Layer 2 theme → Layer 2 real components → Layer 3 scaffold, all the way through, for one pairing) — that's Pattern B, still open.

**Next.js's own Bootstrap (React-Bootstrap) path added later**, right after Vue.js's own Bootstrap addition (see Subject 4) closed the same gap there — `cli/src/scaffold/nextjs-bootstrap.ts`, `scaffold --framework nextjs --bootstrap`. Same base CLI as the Tailwind path (`create-next-app`), driven with `--no-tailwind` instead (confirmed current convention: Tailwind now defaults ON, so this needs the real negation flag) plus a real Sass build wiring in `generate --bootstrap`'s own `_variables.scss`. Real findings, verified against the npm registry/react-bootstrap's own dependency graph/Next.js's own current docs:
- No separate `@popperjs/core` install needed — it's a real transitive `dependency` via `@restart/ui`, confirmed by reading both packages' actual published `package.json`, despite older tutorials suggesting otherwise.
- react-bootstrap ships no `"use client"` directive in its own build (confirmed: open, unresolved upstream issue `react-bootstrap/react-bootstrap#6792`) — any file rendering a real component needs its own directive.
- `next.config.ts`'s `sassOptions.quietDeps: true` (confirmed real passthrough via an actual Turbopack build) silenced the same 300+ legacy-Sass-function warnings from Bootstrap's own source the Vue.js path hit; two warnings survived that pass — this file's own `@import` statements, a separate current Dart Sass deprecation about the at-rule itself, not something `quietDeps` covers since it only targets dependency-sourced warnings. Fixed with `silenceDeprecations: ["import"]` alongside it.
- `bootstrap-icons` installs alongside `bootstrap`/`react-bootstrap` — same package/reasoning as the Vue.js path.

**QA note:** verified with a real end-to-end run (`promote` → `generate --bootstrap --framework nextjs` → `scaffold --framework nextjs --bootstrap`), then a real `next build` (Turbopack) — zero warnings, zero errors — confirming the real brand primary color, both font overrides, the real compiled icon rule, and its font files all present in `.next/static`.

**Next.js's own MUI path added right after** — `cli/src/scaffold/nextjs-mui.ts`, `scaffold --framework nextjs --md2`, closing the very last "not built" line in this file's own tracking. Same base CLI again (`create-next-app --no-tailwind`), but no Sass at all — MUI is CSS-in-JS (Emotion), genuinely different from every other design-language path here. Real findings, verified against the npm registry/MUI's own current docs:
- `@mui/material-nextjs`'s `AppRouterCacheProvider` has a real, Next.js-major-specific subpath (`v13`–`v16-appRouter`, confirmed via the package's actual published `exports` map) — `create-next-app@latest` installs Next.js 16.x today, so this uses `v16-appRouter`, not the `v15-appRouter` MUI's own docs page still shows as its lead example.
- The theme file itself needs `"use client"` (confirmed against MUI's current Next.js guide) — the theme object contains functions (e.g. `breakpoints` helpers) that need to be created directly in client-module space, not passed as a prop across the Server-to-Client boundary. `layout.tsx` itself does NOT need the directive, though — a real, verified difference from react-bootstrap: MUI's own components already ship `"use client"` in their own build (confirmed: MUI's own 2023 blog post on doing exactly this for App Router compatibility), so `page.tsx` needs none either.
- **Two real bugs found and fixed by an actual `next build`, not just typecheck:** (1) a multi-word font family's raw token value already includes literal quotes (`'Source Sans Pro'`) — naively re-wrapping that in a hand-written TS string literal produced invalid syntax (`''Source Sans Pro', sans-serif'`); fixed by stripping pre-existing quotes first and using `JSON.stringify` for safe escaping. (2) `fontWeight` isn't a direct `Typography` prop in current MUI 9 typings — broke overload resolution entirely; fixed with `sx={{ fontWeight: 700 }}` instead.
- `@mui/icons-material` (official, peer-pinned to the same `@mui/material` version) closes the icons gap the same way `bootstrap-icons` did for both Bootstrap paths.

**A real, load-bearing bug found and fixed in the `SDSGT-start` skill while building this:** step 8's flag-picking logic mapped `targetDesignLanguage: "md3"` straight to `--md3` regardless of framework — but `generate --md3` is Kotlin/Compose-only (confirmed by reading `generate/md3.ts`'s own header), nothing web-consumable. Next.js's MD3 and MD2 selections were always meant to both route to MUI (same as Vue.js's own MD3/MD2 → Vuetify routing, and matching `ComponentLibrary`'s own `"mui"` comment in `seed-config.ts`), that Next.js-specific exception just never got written into step 8 until now. Fixed: Next.js + (md3 or md2) always generates with `--md2`.

**QA note:** verified with a real end-to-end run (`promote` → `generate --md2 --framework nextjs` → `scaffold --framework nextjs --md2`), then a real `next build` (Turbopack) — zero warnings, zero errors after the two bugs above were fixed — confirming the real brand primary color present in `theme-palette.ts` and the compiled client bundle.

---

---

## Subject 4 — Layer 3: Vue.js scaffolding (all three design-language paths)

**Built 2026-09-14** (Tailwind and Vuetify paths); **Bootstrap/bootstrap-vue-next path added later the same day**, prompted by the user asking why the seed-input form suggests `bootstrap-vue-next` for Vue.js + Bootstrap when nothing actually vendored/installed it — a real "documented current state, not a permanent rule" gap, not a deliberate scope cut. The user confirmed up front that both original Vue paths should be built in the same session, not staged — Vue + Tailwind (mirroring the Next.js slice) and Vue + Vuetify/Material Design (a genuinely separate pipeline: different official CLI, different theme source, no Tailwind involved at all). See `contracts-and-seeds.md`, "Vue.js scaffold (Layer 3)," for the full written-up contract — this section is the narrower "what broke and how it got fixed" record.

**Bootstrap/bootstrap-vue-next path, real findings (verified against npm registry + Bootstrap's own current docs, not training data):**

- **bootstrap-vue-next has no scaffolding CLI of its own** (confirmed against its real npm package/GitHub source) — this path drives `create-vue` (the same base CLI the Tailwind path already uses) and wires Sass + Bootstrap + bootstrap-vue-next in by hand instead of Tailwind, rather than a fully separate CLI the way Vuetify's path drives `create-vuetify`.
- **bootstrap-vue-next's own real npm `dependencies`** (`reka-ui`, `@vueuse/core`, `@floating-ui/vue`, `@floating-ui/core`) install automatically with the package — no Popper.js needed at all, since bootstrap-vue-next reimplements Bootstrap's interactive JS in Vue rather than depending on Bootstrap's own JS bundle (confirmed by reading the real published `package.json`, not assumed from Bootstrap's own JS-based docs).
- **bootstrap-vue-next's own docs explicitly say Sass variable customization isn't done through the library** — they point at Bootstrap's own "Lean Sass Imports" pattern instead. This pipeline's `generate --bootstrap` already writes exactly the variables partial that pattern needs (`bootstrap/_variables.scss`) — no new generator work, just wiring: a `sass` devDependency, and a `main.scss` importing that partial before `@import "bootstrap/scss/bootstrap"`.
- **Fonts need no Sass at all** — Bootstrap 5.3's compiled CSS exposes `--bs-font-sans-serif`/`--bs-body-font-family` as real runtime CSS custom properties on `:root` (confirmed against Bootstrap's own CSS-variables docs), overridable in a later plain CSS rule with no recompilation — same "plain CSS custom properties, no build-time preprocessing" posture as Vuetify's own `--v-font-body`/`--v-font-heading` treatment. Headings get a direct `h1..h6` selector override instead of a `--bs-headings-font-family` custom property, since Bootstrap only emits that property when `$headings-font-family` is explicitly set in Sass (it's `null` by default).
- **A real, verified build-noise finding**: Bootstrap's own Sass still uses legacy Dart Sass color functions (`red()`/`green()`/`blue()`) and old `if()` syntax, both deprecated by the modern Sass color module — hit as 300+ real deprecation warnings on the first `npm run build`, not a false positive (traced to `node_modules/bootstrap/scss/_functions.scss`, not this pipeline's own generated `_variables.scss`). Bootstrap's own Vite guide documents a `silenceDeprecations` Vite config for most of this; one additional ID (`if-function`) isn't in that doc yet but is the same class of issue, confirmed the same way (real build output).

**QA note:** verified with a real end-to-end run (`promote` → `generate --bootstrap --framework vuejs` → `scaffold --framework vuejs --bootstrap`), then a real `vue-tsc --build` + `vite build`, confirming the real brand primary color (`--bs-primary:#3b82f6`) and both font overrides (`--bs-font-sans-serif`, the `h1..h6` rule) present in the actual compiled CSS output — same bar as the rest of this subject, not a lighter one for a later addition.

**None of the following were guessable from the Next.js precedent** — each is a real thing found by actually running the tools, not assumed from how `create-next-app`/shadcn/ui behaved:

- **create-vue has no `--tailwind` flag and does not auto-install its own dependencies** (`create-next-app` does both). This pipeline installs `tailwindcss`/`@tailwindcss/vite` itself in the same `npm install` call that covers create-vue's own base deps.
- **A real, current version drift**: create-vue's own `--eslint` template locks an `oxlint` devDependency version that has already drifted ahead of `eslint-plugin-oxlint`'s peer range — confirmed by hitting a real `ERESOLVE` against current npm registry state (not a local-machine fluke). Fixed with a project `.npmrc` (`legacy-peer-deps=true`), left in the generated project permanently (not just used transiently for this scaffold step) since the same drift would break the end user's own future `npm install`s too.
- **An absolute `--out` path silently breaks both create-vue's AND create-vuetify's non-interactive behavior**, in two different failure shapes — create-vue drops into a live interactive prompt (printed "Package name:" and created nothing, confirmed by checking the target path was empty afterward); create-vuetify instead silently creates the project under the *calling process's own cwd* instead of the intended path (confirmed by finding a real, unwanted project directory inside this repo's own `cli/` folder after a run — cleaned up, never committed). Both fixed the same way: run from the target's parent directory, pass just the bare basename.
- **shadcn-vue's installation checker needs `@import "tailwindcss"` already present, and reads the import-alias from the ROOT `tsconfig.json`** — create-vue's split-tsconfig template only sets the alias in `tsconfig.app.json`, and a fresh create-vue project's default `main.css` has no Tailwind import at all yet (unlike Next.js, where `create-next-app --tailwind` already established both before shadcn's own `init` ever runs). Fixed by seeding a one-line `@import "tailwindcss";` into `main.css` and writing the alias into the root `tsconfig.json` before calling `shadcn-vue init`.
- **`shadcn-vue add --all` has a real gap**: it vendors the `chart` component's source but never installs its registry-declared runtime dependency (`@unovis/vue` + `@unovis/ts`) — confirmed by a real `vue-tsc` typecheck failure that only cleared once these were installed by hand. Fixed with an explicit follow-up install.
- **shadcn-vue's own default theme loads a live Google Fonts CDN `@import`** (Geist) — a real network request on every page load, unlike shadcn/ui's Next.js flavor (which uses `next/font`, never a CDN import, so there's no equivalent "harmless if unused" file sitting around). Stripped before appending this pipeline's own font override.
- **create-vuetify's own template download is genuinely flaky**: it fetches its template from GitHub at scaffold time (`giget`-based, not bundled in the npm package) and hit real, repeated 504 Gateway Time-outs during verification — confirmed NOT a real GitHub outage (a plain `curl` against the same URL succeeded immediately every time, including moments after a `create-vuetify` failure). Fixed with a retry loop (4 attempts, escalating 3s/8s/15s backoff) around just that one call — cheap insurance for something entirely outside this pipeline's control.
- **Vuetify's own real installed SCSS already reads `--v-font-body`/`--v-font-heading` CSS custom properties** (confirmed by reading the actual installed package source, since the docs site itself is JS-rendered and unfetchable via a plain fetch) — meaning font wiring needed zero Sass, just a plain stylesheet setting two custom properties. Replaced create-vuetify's own default (`unplugin-fonts` fetching Roboto from `@fontsource`) with this pipeline's own real fetched files.
- **Icons resolved for free**: `@mdi/font` comes vendored by create-vuetify's own default preset, closing the last open "icons as assets" gap from `pipeline-plan.md`'s deferred list (Vuetify was the one library still unresolved there).

**QA note:** verification for this subject was real end-to-end CLI runs (`node src/cli.ts scaffold ...` for all three combinations: plain Tailwind, Tailwind+shadcn-vue, Vuetify), each followed by a real `vue-tsc --build` typecheck and a real `vite build`, with brand colors/fonts confirmed present in the compiled output — same bar as Subject 3's Next.js work, not a lighter one for the second framework.

---

## Subject 5 — Layer 3: React Native (Expo) scaffolding (NativeWind + React Native Paper)

**Built 2026-09-14, same session as Subject 4.** The user confirmed both paths should be built together, mirroring Vue's structure — a NativeWind base and a separate React Native Paper (MD3) path. See `contracts-and-seeds.md`, "React Native (Expo) scaffold (Layer 3)," for the full written-up contract. This section is the "what broke and how it got fixed" record, same treatment as Subject 4.

**The single biggest finding, discovered before any code got written:** NativeWind v4 (current stable — confirmed via real npm dist-tags: `"latest": "4.2.6"`, v5 only under `rc`/`preview`) has a hard peer dependency on Tailwind CSS **v3**, not v4 — confirmed by inspecting `react-native-css-interop` (NativeWind's own real dependency), whose actual `peerDependencies` pin `"tailwindcss": "~3"`, a much stricter constraint than NativeWind's own loosely-worded `">3.3.0"`. This meant the React Native scaffold could **not** reuse `generate --tailwind`'s output the way Next.js and Vue.js both do — that generator targets Tailwind v4's CSS-native `@theme` syntax. The fix already existed in this pipeline without new work: `generate --rnr`'s output (`rnr/global.css`, built back on 2026-09-10 for RNR's own theming contract) was already written in exactly the right shape — classic `@tailwind base/components/utilities` directives, raw `"H S% L%"` triplets, `.dark:root` selector — because RNR itself needed Tailwind v3 compatibility for the same underlying reason. `--rnr`'s theme became the base for the NativeWind scaffold regardless of whether RNR's own components ever get vendored (they still can't — see the re-verified blocker above).

**`tailwind.config.js`'s color-extend block is copied from RNR's own real upstream template**, not invented — fetched directly from `founded-labs/react-native-reusables-templates`' `minimal` variant on GitHub (the same org whose CLI investigation is written up above), confirmed to use the exact CSS variable names/shapes `generate/rnr.ts` already writes. This pipeline's own success/warning/info/promo status-role extensions were added on top, same treatment `generate/shadcn.ts` already gives them on web.

**Real bugs/gotchas found by actually running the tools, none guessable in advance:**

- **`create-expo-app` handles absolute paths fine** — confirmed by explicit test, unlike create-vue/create-vuetify (Subject 4). No parent-directory workaround needed here.
- **`blank-typescript` (the chosen base template — smallest real option, avoiding `default`/`tabs`' large amount of Expo's own marketing/demo content) has no web support by default.** A real `expo export --platform web` failed outright asking for `react-dom`/`react-native-web` — but RNR's own real upstream template ships both, confirming a real NativeWind (or Paper) project is expected to support web out of the box, not something invented here. Both scaffolds now install them via `expo install`.
- **`create-expo-app --no-install` means `expo install` can't run first** — it needs the `expo` package already in `node_modules` to read the project's SDK version, and errors with "Cannot determine the project's Expo SDK version" if node_modules doesn't exist yet. Fixed with a plain `npm install` (installing the template's own base deps) before any `expo install` call.
- **NativeWind's own `nativewind-env.d.ts` convention doesn't actually declare a `*.css` module** — confirmed by both a real `tsc --noEmit` failure ("Cannot find module or type declarations for side-effect import of './global.css'") and by fetching RNR's own real template's `nativewind-env.d.ts`, which has the exact same gap (just `/// <reference types="nativewind/types" />`, nothing more). `nativewind/types` only adds `className` props to RN components; it was never meant to cover this. Fixed by adding `declare module "*.css";` directly.
- **`inlineRem: 16` is confirmed necessary by RNR's own real template, not just this project's own prior finding** — their `metro.config.js` sets the exact same key to the exact same value, independent confirmation of the `nativewind-project-setup` skill's own fix.
- **RN Paper's icons are resolved for free on Expo** — confirmed against Paper's own real current getting-started guide: "If you use Expo, you don't need to install vector icons — those are part of the expo package." Closes the last open "icons as assets" gap from `pipeline-plan.md`'s deferred list.

**A real, deliberate scope cut, not an oversight: no font files.** Every other scaffold in this pipeline (Next.js, Vue.js) wires real fonts via a plain CSS `@font-face` block pointing at the `.woff2` files `promote --fonts-dir` already fetches. React Native has no CSS and no `@font-face` — real custom fonts need `.ttf`/`.otf` files loaded via `expo-font`'s `useFonts()`, a different file format and loading mechanism this pipeline's font-fetching (built around Google Fonts' CSS2 API, which only serves `.woff2`) doesn't produce. Both React Native scaffolds fall back to the system font, same "degrades gracefully" treatment every other scaffold already uses when `fontsDir` isn't passed — just unconditional here. Extending font-fetching to also grab `.ttf` files is real, separate, not-yet-scoped work.

**QA note:** same bar as Subjects 3–4 — real end-to-end CLI runs for both paths, each followed by a real `tsc --noEmit` typecheck and a real `expo export --platform web` (the closest available equivalent to a real compiled build in an environment with no iOS/Android simulator), with real brand colors confirmed present in the compiled output (the NativeWind path's CSS bundle; the RN Paper path's JS bundle, since Paper applies its MD3-derived theme colors at runtime the same way Vuetify does).

---

## Subject 6 — Layer 3: Kotlin/Jetpack Compose, resolved as a guide instead of automation

**Decided and built 2026-09-14.** Before any code got written, the user asked which of Kotlin/Android or SwiftUI/iOS to prioritize next. The recommendation was Kotlin, on the reasoning that Gradle offers a real CLI-driveable path where Xcode's own project creation has no non-interactive equivalent at all — that reasoning held up as directionally right, but a closer look found Kotlin's own path has a real gap too, just a different one than SwiftUI's.

**Real research, not assumed — three independent checks, all confirming the same conclusion:**
- **No official `android create <template>` CLI exists.** A first web search surfaced a plausible-sounding `android create empty-activity --name=MyApp` command — re-checked directly against Android's own real, current `developer.android.com/tools` command-line-tools page (which lists every real tool: `sdkmanager`, `avdmanager`, `lint`, `apkanalyzer`, `adb`, build tools, platform tools, the emulator, `jetifier`) and confirmed no project-creation tool exists anywhere in it. The first search result was simply wrong — an old, long-removed `android` tool command from roughly the 2013–2017 era, or a fabrication; either way, not something to build against.
- **`gradle init` is real, but doesn't produce a real Android app.** Verified via an actual local run (installed Gradle 9.7.1 + a real JDK via Homebrew, then `gradle help --task init`): `--type` supports `kotlin-application`, `kotlin-library`, `java-application`, etc. — every one of these is a generic JVM project. No `android-application` type exists. Gradle itself is Android-agnostic; the Android Gradle Plugin (AGP) that turns a Gradle project into a real Android app is a separate Google plugin `gradle init` has no knowledge of.
- **Android Studio itself has no documented headless/batch project-creation mode.** Verified against its own real `developer.android.com/studio/command-line` documentation — every command-line capability documented there is SDK tooling (`sdkmanager`, `avdmanager`, build/platform tools, Gradle itself), never the IDE. File > New > Project's GUI wizard is the only officially documented way to create a new Android project.

**This is a structurally different situation from every other Layer 3 target in this pipeline, not just a harder version of the same problem.** Next.js, Vue.js, and React Native each have a real official non-interactive CLI this pipeline drives directly. Kotlin has none — there is nothing to shell out to, `spawnSync`-style, the way every other scaffold builder in `cli/src/scaffold/` works.

**Decided: a real, filled-in instructions file instead of a `scaffold` command that would have nothing to drive.** This is the same "Pattern A" move already used for Vuetify/React Native Paper's own `SETUP.md` (Subject 2, above) when no automatable install/vendor path exists — just one level up: whole *project creation* instead of "add a library to an existing project." Built as `cli/src/generate/kotlin-scaffold-guide.ts`, called unconditionally whenever `generate --md3` runs (same "always write it, not gated behind `scaffoldMode`" treatment as `SETUP.md`), writing `md3/SCAFFOLD_GUIDE.md` next to `Color.kt`. Real content, not a generic tutorial:
- Real current file locations Android Studio's own "Empty Activity" Compose template creates (`app/src/main/java/<package>/ui/theme/{Color,Theme,Type}.kt`), verified against Google's own current Compose theming documentation.
- **A real, correct simplification worth recording:** the default template's own `Theme.kt` wraps a partial `lightColorScheme(primary = ..., ...)` *builder* call, since its own default `Color.kt` only defines individual color values. This pipeline's own generated `Color.kt` is already a complete `ColorScheme(...)` instance (all 48 real fields — the same shape `generate/md3.ts` has always produced) — so the guide's own `Theme.kt` code is simpler than the template's own default, referencing `LightColorScheme`/`DarkColorScheme` directly rather than re-wrapping them in another builder call.
- **A real, deliberate omission:** the guide's `Theme.kt` skips Android 12+'s "dynamic color" option (`dynamicLightColorScheme(context)`, deriving colors from the device wallpaper) that the default template's own `Theme.kt` usually offers — using it would silently replace this project's real generated brand colors with whatever a given device's wallpaper produces, the opposite of what this pipeline is for. Same "our real values always win over a platform default" principle already applied elsewhere (e.g. avoiding `next/font/google`'s identifier-guessing).
- **A real cross-check, not a coincidence:** the guide references Compose BOM `2026.08.00` (verified current via Android's own BOM-to-library mapping table), which maps to `material3:material3` `1.4.0` — the exact version `generate/md3.ts` has targeted since 2026-09-08, confirmed still correct rather than having silently drifted.
- Conditionally documents the `ElevationOverlay` object only when the user's own shadow tokens are dimension-shaped (same condition `md3.ts` itself already uses to decide whether to emit it) — detected by reading back the already-written `Color.kt` content for the string `"object ElevationOverlay"`, the same "read back what was actually written rather than re-deriving a parallel condition" pattern the Vue/React Native scaffolds already use for their own conditional exports.

**A real bug caught before shipping:** an early draft split a markdown link (`[text]` / `(url)`) and an inline code span across two array-of-lines entries, landing a literal newline between `]` and `(` — which CommonMark does not parse as a link (link text and destination must be adjacent). Caught by actually generating the file and reading the real output, not by inspecting the source array. Fixed by keeping both the link and the code span each on one unbroken line.

**Wired into the `SDSGT-start` skill, not just the CLI:** step 9 (reporting `generate`'s results) now sends `md3/SCAFFOLD_GUIDE.md` explicitly whenever `targetFramework` is `kotlin`, framed as "this is Kotlin's equivalent of step 10," and step 10's own gate no longer lumps Kotlin in with genuinely-unbuilt combinations (Bootstrap-flavored targets, SwiftUI) — it now redirects back to step 9's guide instead of saying "not built yet," since that would undersell what actually happened.

---

## Subject 7 — Layer 3: SwiftUI/Xcode, resolved as a guide instead of automation

**Decided and built 2026-09-14, same session as Subject 6.** After Kotlin's own resolution, the user asked to check whether SwiftUI had the same kind of automated path Kotlin didn't, and to apply the same guide-instead-of-automation treatment if not.

**Real research, verified on a real Xcode 26.4 install on this machine — not assumed from docs, and stronger verification than Kotlin's own research had available (no Android SDK/emulator exists in this environment; a full macOS + Xcode install does):**
- **No `xcodebuild` project-creation action exists.** Read the tool's own real, complete `-help` usage list — build, test, archive, export, download platforms/components, resolve package dependencies, create xcframeworks, manage localization — nothing that creates a new app project.
- **`swift package init` has no iOS-app template type at all.** Verified via its own real `--help`: `--type` supports `library`/`executable`/`tool`/`build-tool-plugin`/`command-plugin`/`macro`/`empty` — every one is command-line/library-shaped. This is a cleaner "no" than Kotlin's own `gradle init` finding: Gradle's tool at least produced something (the wrong shape, a generic JVM app); Swift Package Manager's `init` doesn't attempt an iOS app shape in any of its options.
- **Xcode's own project templates aren't exposed through any documented CLI hook.** Checked for the template directory location older Xcode versions used to expose — not present at that path in Xcode 26 (reorganized/hidden), and no `xcodebuild`/`xcrun` subcommand references templates at all.

**Same resolution as Kotlin: `generate --swiftui` now always writes a real, filled-in guide instead of a `scaffold` command that would have nothing to drive.** Built as `cli/src/generate/swiftui-scaffold-guide.ts`, writing `swiftui/SCAFFOLD_GUIDE.md` — real current default file locations (`<AppName>App.swift`, `ContentView.swift`), the real "Copy items if needed" + target-membership gotchas when dragging generated files into Xcode's navigator (a real, easy-to-miss mistake, not a generic warning), and real `xcodebuild -showdestinations`/`-destination` commands for building from the command line without hardcoding a specific simulator name that would go stale.

**One real technical problem Kotlin's own guide never had, solved with a second generated file, not just prose:** `generate/swiftui.ts`'s own `DesignTokens.swift` is two flat `enum { static let ... }` namespaces (`Light`/`Dark`) with no built-in way to dynamically switch between them — unlike Compose's `ColorScheme` (switched wholesale by Compose itself) or Vuetify's theme object (switched by `createVuetify()` itself), a plain Swift enum-of-static-lets has no equivalent mechanism. Left as prose, every real call site in the user's app would need a manual `colorScheme == .dark ? DesignTokens.Dark.x : DesignTokens.Light.x` ternary — technically usable, but exactly the "theoretically present, not actually usable" gap this project doesn't leave in place elsewhere. So the guide generator also writes a real `swiftui/Theme.swift` — a standard SwiftUI `EnvironmentKey`/`ViewModifier` pattern, with every one of the project's own real token names wired in, extracted by reading back the already-written `DesignTokens.swift` content (same "read back what was actually written" pattern the Vue.js/React Native/Kotlin guides already use for their own conditional exports) rather than re-deriving the token list from the raw JSON a second time. Branches correctly for a single-mode project too (`Theme.current`, a plain constant — no environment switching to wire up when there's nothing to switch between).

**Verified with the real Swift compiler, not just inspected — the strongest verification any guide-generator in this pipeline has had**, since Xcode happens to be installed on this development machine: `xcrun --sdk iphonesimulator swiftc -target arm64-apple-ios17.0-simulator -typecheck DesignTokens.swift Theme.swift` exits 0 for both the light+dark case and the light-only case, and the guide's own exact usage code sample (a `ContentView`/`@main App` pair using `@Environment(\.theme)` and `.withAppTheme()`) was also compiled for real against the generated files and typechecks cleanly.

**A real bug caught before shipping, same class as Kotlin's own markdown-link bug:** an early draft's "What's in here" section described `Theme.swift` as "the dynamic-switching wrapper" unconditionally, even in the single-mode branch where it's actually a plain constant with no switching at all. Caught by generating both branches for real and reading the actual output, not by inspecting the source template. Fixed to branch the wording the same way the rest of the guide already branches on `hasLight`/`hasDark`.

**Wired into the `SDSGT-start` skill exactly like Kotlin's own guide**, plus one more file: step 9 now sends `swiftui/SCAFFOLD_GUIDE.md` (`display: "render"`) AND `swiftui/Theme.swift` (`display: "attach"`, since it's a source file to drop into Xcode, not something to read inline) whenever `targetFramework` is `swiftui`, framed as SwiftUI's own equivalent of step 10. Step 10's own gate now redirects both Kotlin and SwiftUI back to their step-9 guides instead of the generic "not built yet" line used for genuinely-unbuilt combinations.

**With this, Layer 3 has a real answer — automated scaffold or real guide — for every framework this pipeline supports.** Vue.js + Bootstrap got its own real component-library story later the same day (see Subject 4's own Bootstrap addition, above), Next.js + Bootstrap (React-Bootstrap) followed right after (see Subject 3's own addition, above), and Next.js + MUI closed the gap right after that (same Subject 3 addition). **Nothing is left in the "not built" category anymore** — every framework/design-language combination this pipeline's seed input can actually produce has a real scaffold, a real install-and-wire, or a real guide. React Native's own Bootstrap "gap" was never really one — Bootstrap has no React Native option at all, so no seed could ever select it.

---

## A recommended order (starting proposal — confirm with the user before committing)

1. ~~**Subject 1** (fix the skill)~~ — **done, 2026-09-11.**
2. ~~**Pattern A first** (React Native Paper and/or Vuetify)~~ — **done, 2026-09-11.** Confirmed no Layer 3 dependency was needed, as guessed.
3. ~~**The smallest possible Layer 3 slice**, scoped to exactly one framework~~ — **done, 2026-09-12.** Next.js + Tailwind, per the reasoning below. See Subject 3 above for what actually got built (and the real Tailwind dark-mode bug it exposed and fixed along the way).
4. ~~**Pattern B** (shadcn/ui first, then RNR)~~ — **shadcn/ui done, 2026-09-12.** See Subject 2 above for what got built and how each open decision (component list, customization guard, theme wiring order) resolved. **RNR investigated and blocked the same day** — real, documented reasons in Subject 2 above, not a vague "still open." Needs either RNR's own CLI to ship real non-interactive flags, or an Expo/React Native scaffold to exist in this pipeline for other reasons, before it's worth revisiting.
5. ~~**Vue.js Layer 3** (Tailwind+shadcn-vue and Vuetify paths, plus Bootstrap/bootstrap-vue-next added later the same day)~~ — **done, 2026-09-14.** See Subject 4 above.
6. ~~**React Native (Expo) Layer 3** (both NativeWind and React Native Paper paths)~~ — **done, 2026-09-14, same session.** See Subject 5 above. RNR's own real component vendoring was re-confirmed blocked *via `init`* that same session — then unblocked later the same day by testing `add` standalone instead, once Kotlin/SwiftUI's own resolutions prompted asking whether the same pattern applied here (it didn't directly, but the question led to the right retest) — see Subject 2's own updated write-up and the "Pattern B" line above.
7. ~~**Kotlin vs. SwiftUI priority**~~ — **resolved, 2026-09-14.** Kotlin recommended and confirmed with the user first (Gradle's own real CLI-driveable path vs. Xcode having none at all) — but real research found Kotlin's own path has a genuine gap too, just a different shape: no official non-interactive Android/Compose project-creation CLI exists anywhere. Resolved as a guide instead of automation, not deferred — see Subject 6 above.
8. ~~**SwiftUI Layer 3**~~ — **resolved, 2026-09-14, same session.** User asked to check for the same gap Kotlin had and apply the same treatment if found — real research (verified on a real local Xcode 26.4 install) found the same structural gap: no official non-interactive Xcode/SwiftUI project-creation CLI exists either. Resolved as a guide instead of automation, same pattern as Kotlin, plus one real technical addition Kotlin didn't need (a generated `Theme.swift` solving `DesignTokens.swift`'s own lack of built-in light/dark switching) — see Subject 7 above. **With this, every framework this pipeline supports has a real Layer 3 answer** — automated scaffold (Next.js, Vue.js, React Native) or real guide (Kotlin, SwiftUI). Nothing left unstarted at the framework level; only specific design-language gaps remain (Bootstrap now has a real component-library story on Vue.js, added the same day this order was last updated — see Subject 4 — but still not on Next.js, and React Native has no Bootstrap option at all).

This order is a proposal, not a decision already made — the project's own working style (see `CLAUDE.md`) is to confirm architecture forks with the user before committing, and this one has real forks in it (which framework first, which component list, how to detect/mark customization). Treat step boundaries above as natural checkpoints to re-confirm scope with the user, the same way every subject in this project has been confirmed before being built.

---

## New scope addition — components must also push to Figma

**Not previously in scope, added 2026-09-11:** once real components exist in code (Subject 2), they should also be pushed into the *same* Figma file the token push already targets — not just tokens-as-variables, but the components themselves (with variants, auto-layout, and variable bindings back to the pushed tokens), the way a real Figma component library looks.

This is closely related to, but more specific than, the already-deferred "Foundations sheet generator" bullet in `pipeline-plan.md` ("a visual reference sheet inside Figma... deliberately built later, since it's the fragile, iterate-by-screenshot part — variables come first, the visual sheet comes second"). That bullet was about a reference sheet; this is about the actual component set.

**Worth checking before building anything new:** this project already has a Claude Code skill, `figma:figma-generate-library`, described as "Build or update a professional-grade design system in Figma from a codebase... create component libraries, create individual components with proper variant sets and variable bindings, set up theming (light/dark modes)." That sounds like it may already cover a meaningful chunk of this mechanism — investigate whether it can be pointed at this pipeline's own generated components + the already-pushed token variables (`figma-push-plan.json`, the `SDSGT-figma-push` skill) rather than building a parallel push mechanism from scratch. Same "reuse over reinvent" discipline the rest of this project has followed (e.g. `shadcn.ts`'s `--destructive-foreground` reusing `mui-color.ts`'s `contrastText()` rather than inventing a new formula).

**Sequencing note:** this genuinely depended on Subject 2 existing first — there's nothing to push to Figma until real component code (or at least a real component structure) exists somewhere in this pipeline's output. That's now true: shadcn/ui vendoring (Pattern B, above) produces 61 real component files in a real project. Ready to start whenever this is prioritized.

**Started 2026-09-15 — first real proof of concept built, against a real user-provided test file (`FiEnp6EqWy2I6m6ktW65h2`, "SDSGT test file").** Real findings, not assumed:

- **`figma:figma-generate-library` can't literally be "pointed at" this project.** It's built entirely around the `use_figma` tool, which belongs to Figma's *official* OAuth-gated MCP (`mcp__plugin_figma_figma__*`) — a server this project has never authenticated against (confirmed: `use_figma` isn't even a reachable tool yet). This pipeline's real, working connection is the **Southleft** MCP, a different server whose only raw-Plugin-API surface is `figma_execute` — the same mechanism `SDSGT-figma-push` already uses for text/effect styles. The skill's domain knowledge (variables before components, one component at a time, real variant sets, real bindings, screenshot-validate after each step) is still the right playbook — it just has to be manually re-expressed through `figma_execute` rather than reused verbatim.
- **The test file wasn't actually empty** — page contents were empty, but a real prior token push (143 variables, `Tokens - Light`/`Tokens - Dark`, a lime-green brand color unrelated to any seed used this session) already existed at the file level, invisible from a page/canvas check alone. Investigated before doing anything — cleaned up only the one stray empty collection this session's own first (failed) attempt created, left the real pre-existing push untouched, and built the component against those real, already-live variables rather than duplicating a second push.
- **First real component: a full shadcn/ui `Button` variant set** — 5 of its 6 real variants (`default`/`outline`/`ghost`/`destructive`/`link`; `secondary` skipped, see below), each a real Figma component with fills/text-color/corner-radius/padding/font-size/font-family all bound to the real pushed variables (not hardcoded), combined into one real `COMPONENT_SET` with a real `Variant` property (Figma's own native variant mechanism, via `combineAsVariants`) plus a real `Label` TEXT component property. Verified end-to-end, not just visually: created a real instance, swapped both properties (`Variant` → `Destructive`, `Label` → a different string) via `setProperties()`, and confirmed it rendered correctly — a real, usable component, not just a static shape that happens to look like one.
- **`secondary` variant skipped, not faked** — this file's actual token push has no `color/semantic/action/secondary` variable at all, because (per this project's own documented rule) that token only gets emitted when a seed supplies a secondary brand color, and this one didn't. shadcn's own real `bg-secondary` class would be equally broken in a real project built from this same seed. Building a "secondary" button bound to nothing, or to a fabricated substitute, would have been less honest than just not building it.
- **A real bug caught by an actual screenshot, matching a gotcha this project already knew about but had forgotten to apply here**: bound the `destructive` variant's text to `color/semantic/status/error-text`, which rendered invisible (red-on-red). `contracts-and-seeds.md`'s own shadcn theming section already documents this exact trap for `--destructive-foreground` — `error-text` is the *same saturated tone* as `error` itself, meant for colored inline text on a neutral surface, not a contrast color for text sitting on a filled background. Fixed by rebinding to `color/semantic/text/on-primary` (real white, already used correctly elsewhere) instead.
- **Sizing is real-token-bound but not pixel-identical to shadcn's own compiled CSS** — a disclosed, deliberate simplification, not an oversight. shadcn's real `Button.tsx` uses several Tailwind half-step values (`px-2.5`=10px, `gap-1.5`=6px) that have no exact match in SDSGT's own integer-step spacing scale; this build binds to the nearest real token (`spacing/3`=12px horizontal, `spacing/1`=4px vertical) instead of an unbound literal, and only built the `default` text-button size — `h-8`=32px is the one size that matches an SDSGT spacing token (`spacing/8`) exactly. The `xs`/`sm`/`lg`/icon-only sizes are real, separate follow-up work, not done here.

**Turned into a real, automatic mechanism the same session, per explicit user direction: not a separate skill, wired into the existing pipeline.** The user's own framing: "once the scaffold is complete... the pipeline should feed the components into Figma" automatically, not as something invoked later by hand. Built as:

- **`cli/src/scaffold/figma-components-plan.ts`** — a real, generalizable CLI module (not a one-off script for Button specifically) that parses a scaffolded project's *actual* vendored component source (real CVA `variants` extraction via targeted regex, a real shadcn-CSS-var → SDSGT-semantic-path table taken directly from `contracts-and-seeds.md`'s own documented mapping, and real nearest-real-token resolution for values with no exact match) into a `FigmaComponentsPushPlan` — same "CLI computes the plan, the push procedure only replays it" split `promote/figma-plan.ts` already uses for tokens. Deliberately reads spacing/radius/font-size values out of the base platform's own always-written `css/tokens.css` rather than needing a new `--tokens-dir` flag threaded through `scaffold` — one less piece of new CLI plumbing.
- **Wired into `scaffold --framework nextjs --shadcn`** — writes `figma-components-push-plan.json` into the scaffolded project's own root automatically, the same "harmless if unused, costs nothing to compute" treatment `figma-push-plan.json` already gets from `promote`. Verified: a real fresh scaffold run reproduced the exact same plan (bindings, skip reasoning, nearest-token notes) the manual proof-of-concept build produced by hand.
- **`SDSGT-figma-push` extended with a new step 7** (not a new skill — see above) that replays this plan via `figma_execute`, encoding the real, proven technique from the manual build: auto-layout frame per variant, paint-level variable binding (`setBoundVariable("fills", ...)` fails outright — bindings go on the paint object, not the frame field, confirmed live), `combineAsVariants` + re-layout, a real TEXT component property, and mandatory screenshot validation before moving to the next component (catches same-tone fill/text bugs no API call itself would ever error on).
- **`SDSGT-start`'s own step 10 wired to continue into it automatically** when `figmaManaged` is true and a components plan exists — same automatic-continuation treatment the token push already gets after `promote`, not a separate ask.

**Scope still exactly what the manual proof covered — nothing silently expanded**: shadcn/ui's `Button` only, one size, rest states only, `secondary` variant skipped (no fabricated binding) when a seed has no secondary brand color. Extending to more components, more sizes, or other frameworks' vendored source (shadcn-vue's `.vue` files, RNR's React Native source) is real, separate, not-yet-done work — each needs its own real verification before this mechanism is trusted against it, same discipline every other "does this generalize" question in this project has gotten.

### Follow-up — Badge added, proving the parser actually generalizes (2026-09-16)

Picked back up the Figma component push after the full Layer 2 audit closed out, with the user asked directly which of three real directions to extend it in first (more shadcn/ui components, more Button sizes, or new frameworks) — chose more components on the already-proven Next.js + shadcn/ui path, lowest risk since it reuses the exact same parser rather than needing a new one.

**Badge picked as the second component, deliberately** — it's the closest real match to Button's own shape (a real `cva()` call with the exact same six real variant names: default/secondary/destructive/outline/ghost/link), making it the right test of whether `figma-components-plan.ts`'s own mechanism actually generalizes, or was secretly Button-specific code. Fetched and read the real current vendored `badge.tsx` (via a real `scaffold --shadcn` run, not guessed) before writing any parser code — confirmed it now uses `@base-ui/react` rather than Radix (a real, current shadcn/ui registry change), plus two genuinely new patterns Button didn't have:

- **`bg-destructive/10`** — a translucent 10%-opacity destructive fill, not solid. This pipeline's own `classToFigmaVariable` already drops opacity modifiers as a disclosed simplification (`bg-destructive/10` resolves to the same variable as `bg-destructive`) — but doing that here would silently turn Badge's real, readable soft-tint design into a solid destructive fill with destructive-colored text on top, recreating the exact same invisible-text bug Button's own destructive variant needed fixing for originally, just via a different root cause. Applied the same fix (rebind text to `text.on-primary`) for the same protective reason, not a new invention.
- **`rounded-4xl`** — confirmed against the real npm-published `tailwindcss@^4` package's own `theme.css` (`--radius-4xl: 2rem`, 32px). A real, load-bearing check followed: Badge's own real height (`h-5`, 20px) means the visual "full pill" look only survives if whatever radius this pipeline binds to is at least half that height (10px). Checked this holds for the nearest-token match under all four real radius presets (tailwind=12, bootstrap=16, md3=28, md2=16 — every one ≥ 10) before concluding no special "always bind to `radius.full`" rule was needed — the existing nearest-match mechanism already lands on a visually-correct answer by coincidence of the real preset values, not by design, so this was verified rather than assumed to just work.

**Refactored, not duplicated**: extracted the shared variant-parsing logic (class→Figma-variable mapping, the destructive same-tone fix, the secondary-skip-when-no-brand-color logic, the ghost/link text fallback) into a new `buildVariantsFromCva()`, used by both `buildButtonPlan` and the new `buildBadgePlan` — same "extract once a second real consumer needs the identical logic" discipline this pipeline has used repeatedly this session (`read-tokens.ts`'s `nearestDimensionPx`, `scaffold-common.ts`'s `readCssScale`/`nearestScalePx`).

**Verified via a real `generate` → `scaffold` run, output inspected directly**: the real `figma-components-push-plan.json` correctly showed both components, Badge's `outline` variant correctly transparent (no fabricated fill), `destructive` correctly rebound to `text.on-primary`, and the real nearest-token deltas (Badge's own `rounded-4xl` landing 20px off shadcn's exact value, disclosed in the plan's own `notes` array, not hidden) — every value matched what was predicted from reading the real source before writing code, not discovered by trial and error.

**A real, honest distinction, not glossed over**: this verifies the PLAN COMPUTATION only. The `SDSGT-figma-push` skill's own step 7 (the actual replay into a live Figma file) was already written generically (`for each item in plan.components`, no Button-specific branching) — so it should handle Badge with no changes — but a live push for Badge specifically hasn't been proven the way Button's was in the original 2026-09-15 session. Updated the skill's own text to say this precisely: Badge's plan is verified, its live push is not yet, and the first real attempt should get the full screenshot-validate treatment, not be assumed safe just because Button's did fine.

**State after this round**: the Figma component push covers Button and Badge, still only for `nextjs` + `tailwind` + shadcn/ui. More components (Card, Input, Alert, ...), more Button sizes, and other frameworks (shadcn-vue, RNR) remain real, separate, not-yet-done work — this round didn't attempt any of those, only proved the existing mechanism generalizes to a second, genuinely different real component.

### Follow-up, same day (2026-09-16) — Badge's live push done, closing the plan-vs-live gap the section above left open

Ran the actual replay: a fresh `promote` → `generate` → `scaffold` produced a real `figma-components-push-plan.json` with both components, then `SDSGT-figma-push`'s step 7 pushed Badge into the real "SDSGT test file" (`FiEnp6EqWy2I6m6ktW65h2`) already holding Button and its 286 real token variables from the 2026-09-15 session (nothing needed recreating there — checked via `figma_get_variables` first rather than assumed).

**Built and verified exactly per the skill's own procedure**: all 5 real variants (Default, Destructive, Outline, Ghost, Link — Secondary skipped, no secondary brand color in this seed, same as Button) as auto-layout frames bound to the plan's real variable names, combined into a `Badge` component set, placed in the existing `Components` section below Button (no overlap). A first screenshot showed every variant rendering correctly and legibly — no same-tone fill/text collision (Badge's Destructive variant uses `status/error` fill + `text/on-primary` text, a different pairing than the trap Button's Destructive hit originally, but confirmed clean here too). A real instance was created, `setProperties()` used to swap both the `Variant` and `Label` properties live, and a second screenshot confirmed the swap actually took (Destructive fill, "Test swap" label) — proving the component behaves like a real component, not just a static shape.

**One real bug hit and fixed during this run, now folded into the skill itself**: the first `setProperties()` call used the plan's plain property name (`"Label"`) and failed — Figma auto-suffixes explicitly-added TEXT/BOOLEAN component properties with `#<nodeId>` (`"Label#15:0"`), while the variant axis (derived from the `Name=Value` naming convention) keeps its plain name. Because the instance had already been created before that call threw, the failed attempt left an orphaned extra Badge instance sitting on the canvas — caught on the final full-page screenshot (a stray sixth "New" pill below the row), found via a node listing, and removed. Updated `SDSGT-figma-push`'s step 5 with both the property-key lookup fix (`Object.keys(instance.componentProperties)`, matched by prefix) and the "clean up on a failed swap" instruction, so this doesn't have to be rediscovered on the next component.

**State after this round**: both components this plan covers — Button and Badge — are now proven live, not just plan-computed. Updated `SDSGT-figma-push`'s step 7 header to drop the "Badge not yet proven live" caveat. More components, more sizes, and other frameworks remain real, separate, not-yet-done work, same as before.

### Follow-up, 2026-09-17 — Toggle and Alert added, and a real same-tone-trap bug caught before it shipped

Asked to keep going with more Next.js + shadcn/ui components. Rather than pick arbitrarily, checked every real `cva()`-based component in a fully-vendored (`shadcn add --all`) test project first (grepped all ~62 vendored files for `cva(`, found 15, then read each one's own `variants: {}` block). Most either have no size-independent color "variant" axis at all (Input, Checkbox, Switch, Label, Separator — single-state components) or are compound/multi-part components that don't fit this plan's "one flat frame + one text label" shape (Card, Dialog, Sheet, Table, Sidebar, Field, Item — real, separate, larger work). Two fit cleanly enough to build now: **Toggle** (fits the existing mechanism with zero new special-casing) and **Alert** (needed one genuinely new mechanism).

**Toggle** — real current source (`h-8 min-w-8 px-2.5`, `rounded-lg`, `text-sm`, identical shape to Button's own current default size) has an explicit `border-input` class on its `outline` variant, so it parses correctly with no changes to `buildVariantsFromCva` at all. Its real `cva()` also has a `size` axis (default/sm/lg) — ignored here, same disclosed "one size only" scope Button itself already has.

**Alert** — a genuinely new real finding, not a copy-paste addition: its real source (`rounded-lg border px-2.5 py-2 text-sm`) has a bare `border` utility with NO explicit color class in either variant string — the first component checked where a visible stroke doesn't come from an explicit `border-*` class at all. Confirmed via a real scaffolded project's own compiled `globals.css` that shadcn's own generated base layer (`@layer base { * { @apply border-border ...} }`) applies `border-color: var(--border)` to every element unconditionally — so Alert's real border IS `border.default`, just never spelled out per-variant the way every other component checked so far does. Handled as a deliberate post-process (set `strokeVariable` to `border.default` for every variant that didn't get one from the class scan), not a change to the shared parser's own logic, since this is a real, different case, not a gap in what the parser was built to catch. Also real, disclosed, narrower scope: Alert's actual shadcn component is compound (`Alert` + `AlertTitle` + `AlertDescription` + `AlertAction`) — this plan represents only the title, the same "one flat frame, one text label" shape every component built so far uses; `AlertDescription` isn't modeled.

**A real same-tone-trap bug caught by inspecting the generated plan JSON before any live push, not assumed safe from the code alone**: the existing destructive-variant fix (rebind text to `text.on-primary` whenever a variant is named `"destructive"`) fired unconditionally on the variant NAME, which was correct for Button/Badge (both really do have a destructive-COLORED FILL, so white text is the right fix) but produced `text/on-primary` (white) for Alert's real destructive variant too — which keeps a NEUTRAL `bg-card` fill and only changes the TEXT color to red. White text on a light neutral card would have been a real, invisible/low-contrast combination — the exact class of bug this fix exists to PREVENT, shipped by the fix itself for a component it was never checked against. Fixed by gating the rebind on the FILL actually resolving to `status.error`, not just the variant's name — re-verified Button/Badge's own destructive variants are unaffected (`text.on-primary` still correct, since their fills are still destructive-colored) and Alert's is now correctly `status.error` text on a neutral fill.

**Verified**: the corrected plan JSON (Alert's `Destructive` variant: `fillVariable: background.surface`, `textVariable: status.error`, matching the real component); a real `next build` using both new components in a real page (`Toggle`/`Toggle variant="outline"`, `Alert`/`Alert variant="destructive"` with real `AlertTitle`/`AlertDescription` children); a live browser screenshot confirming the rendered result matches — black title on a bordered card by default, red title+description text on destructive, both Toggle variants visually correct (transparent default, bordered outline).

**State after this round**: plan computation, a real compiled build, and live browser rendering were all verified for Toggle and Alert first; once the Desktop Bridge reconnected the same day, both went through the full live push → screenshot → instance-swap → screenshot cycle too, confirmed against the real "SDSGT test file" already holding Button/Badge — including confirming live (not just in the plan JSON) that Alert's same-tone-trap fix actually renders readable red text on both variants. All four components — Button, Badge, Toggle, Alert — are now proven live, not just plan-computed. Updated `SDSGT-figma-push`'s step 7 and `SDSGT-start`'s own scope disclosure to drop the "not yet proven live" caveat for Toggle/Alert. The Figma component push now covers 4 real, live-proven components on Next.js + shadcn/ui; more of shadcn/ui's own component set, other sizes, and other frameworks remain real, separate, not-yet-done work, same as before.

### Follow-up, same day (2026-09-17) — Input and Textarea added, proving a SECOND real component shape, not just a fifth/sixth copy

Asked directly which direction to take "harder Next.js components" — chose extending the parser to a genuinely new component shape over picking easier wins, since the file header had already named two real buckets left unattempted: components with no size-independent color variant axis at all (Input, Checkbox, Switch, Label, Separator), and compound/multi-part components (Card, Dialog, Table). Checked real source for both buckets before committing to one: the no-variant bucket splits further into components that still fit the existing "bordered/filled box" shape (Input, Textarea — real classes are `rounded-lg border border-input bg-transparent px-N py-N text-base`, structurally identical to Button/Badge/Toggle/Alert's own shape, just with no `cva()` variant to extract) versus ones with a meaningful two-state visual difference embedded directly in Tailwind `data-*:` variants instead of `cva()` (Checkbox's checked/unchecked, Switch's on/off) — a third, different parsing problem, deferred rather than attempted in the same pass as the first two.

**Built**: `scanClassesForColors` — reuses the exact same `classToFigmaVariable` class-to-token mapping `buildVariantsFromCva` already uses, applied to ONE plain class string instead of a parsed `cva()` axis. `buildInputPlan`/`buildTextareaPlan` wrap the result in a plan with exactly one variant (`"Default"`) — deliberately not a new parallel schema, since `SDSGT-figma-push`'s own replay mechanism (`for each item in plan.components`) needed zero changes to handle it; a single-value "Variant" property is also a real, common pattern in real design systems, not just a workaround.

**A real, deliberate representational choice, disclosed rather than silently picked**: Input/Textarea's own base classes have no explicit text color for TYPED content (it would inherit `text.primary` from shadcn's own `body { @apply text-foreground }` base rule) — but an at-rest field shows its PLACEHOLDER, not typed text, so both are represented using `placeholder:text-muted-foreground`'s real target (`text.secondary`) instead. `text-base` (16px) is also a real, new-to-this-file font-size target — every component built before this used `text-sm` (14px) exclusively.

**Verified**: the generated plan JSON (no fill, `strokeVariable: border/default`, `textVariable: text/secondary`, `fontSizeVariable: typography/primitive/fontSize/16` for both); a real `next build` using both in a real page; a live browser screenshot matching (bordered box, gray "Placeholder" text); a live Figma push through the full build → screenshot → instance-swap → screenshot cycle, same bar as every other component. Final full-page screenshot confirmed all six component sets (Button, Badge, Toggle, Alert, Input, Textarea) sit cleanly with no orphans or overlaps.

**State after this round**: the Figma component push covers 6 real, live-proven components on Next.js + shadcn/ui, across two distinct, proven-generalizable shapes (multi-variant `cva()` components, and single-state no-`cva()` box components). Real, disclosed, not attempted this round: Checkbox/Switch's own checked/unchecked `data-*:`-variant parsing (a third shape), and any compound/multi-part component (Card, Dialog, Table, Sidebar — need an actual schema change for multiple text children/nested frames, not just a new `build<X>Plan` function).

### Follow-up, same day (2026-09-17) — closing Alert's own disclosed AlertDescription gap, one step toward compound components without building the whole thing

Checked Checkbox/Switch and Card as candidates for the next real increment before picking one. Checkbox/Switch turned out to be a WORSE fit than first assumed: both are genuinely icon-shaped (a tiny fixed-size square, no internal text at all in real usage — a `<Label>` sits OUTSIDE them as a sibling) and Checkbox's checked state relies on drawing a checkmark glyph, which this pipeline has no mechanism to represent from tokens alone (icons are separate, out-of-scope work). Card turned out to be a real 6-part compound component (`Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardAction`/`CardContent`/`CardFooter`) using a dynamic `--card-spacing` CSS custom property and a `ring-1` border instead of a standard `border-*` class — a genuinely bigger schema change than one pass should attempt.

Picked a smaller, real, already-scoped target instead: closing Alert's own previously-disclosed gap (its real `AlertDescription` second text line was never modeled). This is real progress toward the "compound component" bucket — proving a second text line under one frame works end-to-end — without claiming to solve compound components generally.

**Built**: `FigmaComponentPlanItem` gained four new optional fields (`secondaryTextVariable`, `secondaryFontSizeVariable`, `secondaryLabelPropertyName`, `defaultSecondaryLabel`) — deliberately SHARED across every variant, not per-variant like `textVariable`, since a naive first pass at Alert's real `AlertDescription` source revealed it CAN differ by variant (`*:data-[slot=alert-description]:text-destructive/90` tints the description red too on the destructive variant) but that class sits behind a `*:data-[slot=...]:` selector-prefix this parser's simple regex was never built to match. Disclosed rather than silently wrong: the description always renders in `text.secondary`, a real, deliberate simplification, not a missed parse.

`buildAlertPlan` now sets all four fields from Alert's real `AlertDescription` values (`text-sm text-muted-foreground` → `text.secondary`, fontSize 14, matching the title's own size). `SDSGT-figma-push`'s step 7 build code updated to wrap the primary text in a VERTICAL inner stack frame with the secondary text appended below it, only when `secondaryTextVariable` is present — every other component's build path is completely unaffected (checked live: Button/Badge/Toggle/Input/Textarea all still render as single-line, untouched). A real, necessary fix found while writing the replay code, not shipped broken: `variant.findOne(n => n.type === "TEXT")` becomes ambiguous the moment a variant has two text nodes — fixed by naming each node (`"PrimaryText"`/`"SecondaryText"`) at creation time and finding by name, not just type.

**Verified**: the real generated plan JSON; a real live Figma push (removed the old single-line Alert component set first, rebuilt with the new two-line structure) — screenshot confirmed both variants show a bold title + smaller gray description stacked correctly, destructive tinting the title red while the description stays gray as designed; a real instance-swap test confirmed all THREE component properties (`Variant`, `Label`, `Description`) work independently. Final full-page screenshot confirmed no orphans.

**State after this round**: Alert now fully represents its real compound structure (title + description), closing its own previously-disclosed gap. The underlying mechanism (an optional second text line, shared across variants) is reusable for any future component with the same shape, but is NOT general compound-component support — no nested sub-frames beyond the one text stack, no more than two text lines, no per-variant secondary color. Card and any other genuinely multi-part component remain real, separate, larger work.

### Follow-up, same day (2026-09-17) — a real radius bug found while scoping Card, affecting every already-shipped component

Investigating Card's own `rounded-xl` class (to get its real px target right before building anything) led to checking shadcn's actual `@theme inline` radius mapping directly, rather than reusing the "rounded-lg = 8px" assumption every component built so far had baked in. Real current mapping: `--radius-sm: calc(var(--radius) * 0.6)`, `--radius-md: calc(var(--radius) * 0.8)`, `--radius-lg: var(--radius)`, `--radius-xl: calc(var(--radius) * 1.4)` — `--radius-lg` is a DIRECT, EXACT alias to `var(--radius)`, which this pipeline itself sets to `radius.md`'s own resolved value. For the `tailwind` preset, `radius.md` is 6px and `radius.lg` is 8px — two DIFFERENT real values.

Every one of Button/Toggle/Alert/Input/Textarea's `rounded-lg` binding used `nearestToken(8, radius, "radius")` — a nearest-VALUE search against a hardcoded guess of 8, which found `radius/lg` (8px) as a coincidentally exact numeric match. **Confirmed live, not just reasoned from CSS text**: a real Playwright check on an actually-rendered Button showed `getComputedStyle(btn).borderRadius === "6px"`, not 8px — the code's own delta-check (`if (match.deltaPx > 0)`) never caught this because it was measuring distance from the WRONG target, not the real one. A genuine 2px mismatch had been live in Figma since Button was first pushed on 2026-09-15, undetected for two full sessions of work on other components.

**Fixed**: added `exactRadiusMd()`, binding directly to `radius/md` — a guaranteed real alias, never a nearest-value guess — for every `rounded-lg` class (all five affected components). Badge's own `rounded-4xl` is unaffected (a real, separate Tailwind constant, not derived from `--radius` at all, already correctly handled by `nearestToken`). **Re-verified live**: regenerated the plan (all five now show `radius/md`, confirmed `radius/md`'s own resolved value is 6px, matching the real browser check exactly) — then updated the ALREADY-PUSHED Figma components in place (re-bound each existing variant frame's four corner-radius properties to `radius/md`, rather than deleting and rebuilding, since only the radius binding needed to change) and screenshotted the result to confirm the visual change took effect.

**A real, useful precedent for future components**: don't assume a Tailwind utility class's real rendered value from its OWN name or Tailwind's stock defaults — shadcn's `@theme inline` block can and does remap radius (and, previously discovered, spacing/shadow) classes to reference this pipeline's own tokens through a formula, not always a 1:1 passthrough. Check the REAL compiled mapping before picking a numeric target, the same discipline already applied to spacing/shadow, now also applied to radius.

### Follow-up, same day (2026-09-17) — Card built: real compound-component support, not just a bigger second-text-line

With the radius bug fixed, built Card — the real target the Alert-description work (earlier the same day) was explicitly a step toward, not a replacement for.

**Real, deliberate scope, decided before writing any code**: Card's actual API is 6 parts (`Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardAction`/`CardContent`/`CardFooter`). Modeled only `CardHeader` (Title + Description, reusing Alert's own text-stack mechanism) and `CardContent` (one body line) — `CardFooter` (a separate `bg-muted/50 border-t` treatment) and `CardAction` (pure positioning) are real, disclosed cuts, not oversights.

**Two genuinely new parsing problems, both solved by binding to an exact real value instead of a nearest-guess, not by approximating harder:**
- `--card-spacing: --spacing(4)` — Tailwind v4's own arbitrary-function syntax for "4 units of the base spacing scale," the exact same real quantity this pipeline's own `spacing/4` token represents. Bound directly, confirmed `spacing.json` has a `"4"` key under every preset (including `bootstrap`'s narrower 0-5 range) before relying on it.
- `rounded-xl`: unlike `rounded-lg`'s real 1:1 alias (the bug just fixed above), shadcn's real `--radius-xl: calc(var(--radius) * 1.4)` is a genuine multiplier — computed the target from `radius.md`'s own REAL resolved value (not a hardcoded guess) and used `nearestToken` correctly, the same discipline the radius fix just re-established.
- `ring-1 ring-foreground/10`: not a standard `border-*` class, and this parser can't bind a partially-transparent color at all — approximated as `border.subtle`, disclosed plainly, not silently dropped or faked as a solid color.

**A real, deliberate schema decision**: rather than force Card into the existing flat "one frame + one/two text lines" shape, added `sections`/`sectionGapVariable` to `FigmaComponentPlanItem` — an ordered list of horizontally-padded text stacks, laid out vertically inside the component's own outer frame. Purely additive: every one of the six existing components is completely unaffected (still `undefined` for all of them), and `SDSGT-figma-push`'s replay mechanism branches ONLY on whether `sections` is present, not on which component it is.

**A real, self-inflicted risk caught and guarded against, not just accepted**: Card's own plan builder doesn't dynamically parse `cardSource` the way every other builder does — there's no `cva()` axis or plain-class regex that could extract a 6-part sub-component API, a dynamic CSS custom property, and a `ring-*` class. The real values are hardcoded from a one-time real-source read. Given Button's own source already drifted once this session (caught, then found to be a false alarm on inspection — but a real class of risk regardless), added a structural sanity check (`buildCardPlan` now throws loudly if real markers like `"rounded-xl"`, `"function CardHeader"`, etc. disappear from the source) rather than trust the hardcoded values silently forever.

**Verified**: the real generated plan JSON; a real `next build` plus a live Playwright `getComputedStyle` check showing `8.4px` — an exact match to the computed `radius.md × 1.4` target, not a coincidence; a live Figma push (new component, no prior version to remove) through the full build → screenshot → instance-swap → screenshot cycle — screenshot confirmed the vertical Header/Content layout matches the real rendered page closely, and a real instance-swap test confirmed all three independent text properties (`Title`, `Description`, `Content`) bind and update correctly. Resized the "Components" section to fit Card's own real height, since it initially overflowed the section's original bounds.

**State after this round**: the Figma component push covers 7 real, live-proven components on Next.js + shadcn/ui, across three distinct, proven-generalizable shapes (multi-variant `cva()`, single-state no-`cva()`, and now real compound/multi-section). Real, disclosed, not attempted: Card's own `CardFooter`, Checkbox/Switch's checked/unchecked parsing, and any component needing MORE than text (nested interactive sub-components — Dialog, Sheet, Table, Sidebar).

## New scope addition — geometry fidelity for the five "installed and wired" libraries

**Found while scoping the Figma component push, same session, 2026-09-15:** asked directly whether the design system was really "applied" to Vuetify/RN Paper/bootstrap-vue-next/React-Bootstrap/MUI the same way it is for vendored libraries. Checked rather than assumed — real answer, at the time: **color only.** None of the five had ever had their real radius or spacing/padding bound to this pipeline's own presets; every one used the library's own hardcoded default geometry regardless of what corner-roundness/spacing-rhythm choice a seed made. This was true in the real, already-shipped app, not just a hypothetical Figma-illustration gap.

**User's explicit policy, given directly:** either close the gap for real, or disclose it plainly at both the point of choice (seed input) and the point of application (generate/scaffold) — never let Figma or the real app show a property that looks token-driven but isn't.

**Fixed, same session, each verified with a real build:**
- **React-Bootstrap / bootstrap-vue-next** — `generate/bootstrap.ts` now also writes `$btn-padding-y`/`$btn-padding-x`, bound to the nearest real spacing token (a real `nearestSpacingPx` resolver, same "closest real value, never fabricate one" approach the Figma component parser already uses). Radius was already bound (existing work). Verified: a real `next build`'s compiled CSS showed the exact expected `--bs-btn-padding-x`/`-y` values.
- **MUI** — `scaffold/nextjs-mui.ts` now sets `shape: { borderRadius }` in `createTheme()`, bound to `radius.md` (real single value MUI's own components all read directly). Verified: the generated `theme.ts` source shows the exact real token value; a real `next build` compiled clean.
- **Vuetify** — genuinely better than expected. Real research found `create-vuetify`'s own default template already ships a real `src/styles/settings.scss` (previously just a commented-out example) AND a real `sass-embedded` dependency, with `vite-plugin-vuetify`'s `styles.configFile` option already wired to it — the whole Sass pipeline needed for this already existed, unused. `$border-radius-root` is Vuetify's own real base Sass variable (`$rounded: (sm: root/2, null: root, lg: root*2, xl: root*6, ...)` — confirmed against Vuetify's own real scale) that every `rounded-*` class and component default derives from. `scaffold/vuejs-vuetify.ts` now writes real content into that already-present file. Verified with a real `vite build`: the compiled `.v-btn` rule showed the exact real pixel value — genuinely pixel-exact, not a nearest-preset approximation the way this looked likely to require before checking.
- **React Native Paper** — `generate/rn-paper.ts` now sets `roundness` in both `LightTheme`/`DarkTheme`, calibrated as `radius.md / 5` — the `5` confirmed against Paper's own real `Button.tsx` source (`borderRadius = (isV3 ? 5 : 1) * roundness`, 5 for the MD3 theme this generator targets). **A real, structural ceiling, not a partial fix awaiting completion**: Paper's `roundness` is a single global value every component multiplies by its OWN different internal factor — this calibration guarantees Button matches exactly, but Card/Chip/TextInput/etc. each derive a different, non-matching radius from the same value, and there is no per-component override this generator can set instead without individually overriding every component's own style, not done. Verified: the generated `theme.ts` shows the exact real token-derived value, and a real `tsc --noEmit` against a scaffolded project confirmed Paper's own real `MD3Theme` type accepts it.

**Still real, disclosed, not fixed**: Button padding for all four of MUI/Vuetify/RN Paper (React-Bootstrap's is now fixed); Bootstrap-family's `$btn-font-size`; and — found only while writing the disclosure text, a genuinely new finding, not previously flagged — the broader typography *size* scale for MUI and Vuetify (font *family* is bound for both; MUI's h1–h6/body pixel sizes and Vuetify's own type scale are still each library's own defaults, never this pipeline's type-scale tokens). None of these are fabricated or silently assumed fixed — see the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the exact current wording, now disclosed at both seed-input (row 4) and apply-time (step 8/10).

### Follow-up, same day (2026-09-15) — the same class of gap also exists for the two VENDORING libraries, and was silent, not disclosed

The section above only ever covered the five "installed and wired" libraries. Right after, a **separately reported, already-confirmed-with-a-real-build bug** turned up the same "looks token-driven, isn't" shape in shadcn/ui, shadcn-vue, and (differently) RNR — real vendored component *source files*, not installed packages, and arguably a more important case: silent for every project that picks a spacing-rhythm preset that doesn't happen to numerically coincide with Tailwind's own native 4px scale, across two frameworks at once.

**The bug:** Tailwind v4's `@theme` block resolves any spacing utility class that isn't an explicit named override (every fractional class — `px-2.5`, `gap-1.5` — plus any integer key a preset's own `spacing.json` doesn't happen to list) via its own base `--spacing` variable, computed as `calc(var(--spacing) * N)`. `generate/tailwind.ts` had only ever written named per-key overrides (`--spacing-0`, `--spacing-4`, ...), never that base variable — so real, unmodified vendored shadcn/ui and shadcn-vue component source (`button.tsx`, using exactly these classes) silently used Tailwind's raw 4px default regardless of the project's chosen spacing preset. The "tailwind" preset only ever *looked* correct, by coincidence (its own values happen to equal Tailwind's real defaults) — there was never an actual binding, for any preset.

**A second, load-bearing discovery made while fixing it:** the obvious fix location (`generate/tailwind.ts`'s own `theme.css`) turned out to be **dead code on the actual `--shadcn` scaffold path** — `scaffold/nextjs.ts`/`scaffold/vuejs.ts` never `@import` it once `--shadcn` is passed; only the plain, no-component-library Tailwind path does. The file that actually reaches a real shadcn/shadcn-vue build is `generate/shadcn.ts`'s own `theme.css` (appended straight into the scaffolded `globals.css`/`main.css`). Fixed there instead, once this was found by tracing the real scaffold code, not assumed from the generator's name.

**Fixed** (`generate/tailwind.ts`'s `computeLinearSpacingConstant` + `generate/shadcn.ts`, 2026-09-15), for every spacing preset that's genuinely linear (every key's px value = key × one constant — true today for "tailwind"/"md3"/"md2," all 4px/step; false for "bootstrap," a 4x/4x/5.33x/6x/9.6x per-step scale, not a constant one). Verified three ways, all with real builds: (1) a real compiled Next.js build showing the base `--spacing` var correctly set; (2) a real browser's `getComputedStyle` on a real, unmodified vendored `Button` component (both `size="sm"`/`size="default"`) matching a synthetic non-4px linear constant exactly, for both Next.js+shadcn/ui and Vue.js+shadcn-vue; (3) the non-linear "bootstrap" case re-verified unchanged (still Tailwind's raw default, honestly, not silently wrong) with no regression. See `contracts-and-seeds.md`'s "shadcn/ui theming" section for the full writeup, and `generate/tailwind.ts`'s own file header for the mechanism.

**Disclosed, not fixed, for RNR** — checked independently rather than assumed to generalize (this project's own standing discipline). NativeWind here is pinned to **Tailwind v3** (a hard peer-dependency requirement, not a choice — see `scaffold/react-native.ts`'s file header), a fully-enumerated JS config object, not v4's CSS base-multiplier system — so the v4-specific bug above literally can't occur the same way. But a related, arguably worse gap is real: `scaffold/react-native.ts`'s `writeTailwindConfig` has never overridden `theme.spacing` at all — every RNR/NativeWind project uses Tailwind v3's own stock default spacing scale unconditionally, for every spacing class, regardless of the chosen preset. A real fix is possible in principle (v3's `theme.spacing` isn't restricted to linear scales the way v4's base multiplier is) but is separate, larger, not-yet-scoped work — disclosed in the generated `AGENTS.md` and the `SDSGT-start` skill instead of attempted here.

### Follow-up, 2026-09-16 — closing the rest of Layer 2's disclosed geometry gaps

Picked back up from the 2026-09-15 "still real, disclosed, not fixed" list above: Bootstrap-family's `$btn-font-size`; MUI's and Vuetify's button padding and full typography size scale; and RNR's `theme.spacing`. Confirmed with the user first that the order should be button-padding-shaped fixes (Bootstrap/MUI/Vuetify), then RNR spacing, then typography size — followed that order. Every fix below was verified with a real compiled build (and, for the two live-cascade questions that a successful build alone can't answer, a real Chrome/Playwright computed-style check), not just inspected.

- **Bootstrap-family `$btn-font-size`** — `generate/bootstrap.ts` now also writes `$btn-font-size`, bound to the nearest real type-scale `fontSize` primitive. Verified against the real npm-published `bootstrap@5.3.8` tarball's own `scss/_variables.scss`: real default is `1rem` (16px), via `$input-btn-font-size` → `$font-size-base`. Verified with a real `next build`: compiled `--bs-btn-font-size` showed the exact expected value across the default/`-sm`/`-lg` size variants. **Nothing left open for React-Bootstrap/bootstrap-vue-next's geometry fidelity.**
- **MUI button padding** — `scaffold/nextjs-mui.ts` now sets `components.MuiButton.styleOverrides.root`, bound to the nearest real spacing tokens for MUI's own real medium/contained default (`padding: '6px 16px'`, confirmed by reading the real npm-published `@mui/material@9.4.0` tarball's own compiled `Button.js`). A real, disclosed narrower scope: only reaches medium/contained — MUI's `outlined`/`text` variants and every `size="small"`/`size="large"` combination each carry their own separate literal padding this single override doesn't touch. **Verified live, not just build-checked**: a real `next build` + a real Playwright/Chrome `getComputedStyle` check on the rendered default Button showed the token-bound `4px`/`16px`, not MUI's own `6px`/`16px` literal — confirming MUI's own `overridesResolver` cascade order (`styles.root` first, more specific variant/size entries second, but only when the theme actually defines a matching one) behaves as expected for a project that only overrides `root`.
- **MUI typography size scale** — `theme.typography`'s `h1`–`h6`/`subtitle1`/`subtitle2`/`body1`/`body2`/`button`/`caption`/`overline` each now bind to the nearest real type-scale token, targeting MUI's own real per-variant default px size (h1=96, h2=60, h3=48, h4=34, h5=24, h6=20, subtitle1/body1=16, subtitle2/body2/button=14, caption/overline=12 — read from the same real `createTypography.js` source). A real, disclosed consequence of this pipeline's own curated 8-value type-scale (vs. MUI's 13-variant one): h1 and h3 have no nearby token of their own and both resolve to the same `display` value — an honest nearest-match outcome, not a bug. **Verified live**: a real `next build` + a real Chrome computed-style check on rendered `h3`/`body1` text showed `60px`/`16px` exactly.
- **Vuetify button padding** — a structurally different fix than Bootstrap/MUI's, because Vuetify's real default button has **no vertical padding of its own at all**: `padding: 0 roundEven($button-height / $button-padding-ratio)` (confirmed by reading the real npm-published `vuetify@4.2.1` tarball's own `VBtn/_variables.scss` + `_mixins.scss`) — vertical sizing comes entirely from `$button-height` (36px default). Rather than also resizing the button (a bigger visual change than a padding fix should make), `vuejs-vuetify.ts` solves for `$button-padding-ratio` alone, holding the real 36px height fixed: `ratio = height / nearestSpacingToken(16)`. This lands EXACTLY (not approximately) on the target, since `roundEven` (`2 × round(val × 0.5)`) always returns this pipeline's own multiples-of-4 spacing values unchanged. **Verified against a synthetic 18px target, not just a real preset** — every real spacing preset happens to already include an exact 16px step, which would make the fix indistinguishable from Vuetify's own coincidental default (the same "only looks token-driven by coincidence" risk the earlier Tailwind v4 shadcn bug had) — a real `vite build`'s compiled `.v-btn--size-default` rule showed the exact `padding:0 18px`, while `.v-btn--stacked`'s own separate `$button-stacked-padding-ratio` was confirmed untouched.
- **Vuetify typography size scale** — Vuetify's real `$typography` Sass map (15 real MD3 role names — `display-large` … `label-small` — each with its own real default px size) is built via a true recursive `map-deep-merge` (confirmed by reading the real source, `styles/tools/_functions.sass`). `vuejs-vuetify.ts` now forwards just each role's `'size'` sub-key, bound to the nearest real type-scale token, leaving weight/line-height/letter-spacing/font-family untouched. Verified with a real `vite build`: the compiled `.text-title-small`/`.text-display-medium`/etc. utility classes, at every real Vuetify responsive breakpoint tier, showed the exact overridden sizes (e.g. `.text-title-small{font-size:12px}`, overridden from Vuetify's own real 14px default).
- **RNR `theme.spacing`** — a real, non-obvious finding changed the shape of this fix from what was originally planned (recreate Tailwind's full standard scale from a linear unit, mirroring the v4 fix): three of this pipeline's four spacing presets (`tailwind`/`md3`/`md2`) are, by this pipeline's own design (MD3/MD2 have no spacing scale of their own, so they use Tailwind's under the hood — see `contracts-and-seeds.md`'s "Spacing" row), already numerically IDENTICAL to Tailwind v3's own real default scale (key N = N × 4px, confirmed against every entry in the three preset JSON files) — so `theme.spacing` being un-overridden was never actually wrong for those three. Only `bootstrap` (non-linear, keys 0-5 only) was ever really mismatched. `scaffold/react-native.ts`'s `writeTailwindConfig` now extends (not replaces) `theme.spacing` with whatever real keys the run's own `spacing.json` defines — correct either way: an explicit no-op restatement for the three linear presets, a real fix for `bootstrap`. **Verified against a real vendored RNR project** (`--rnr`, real `add -a --styling-library nativewind` run): the real `bootstrap`-preset button.tsx's own classes (`px-3`, `px-4`, `gap-2`) were confirmed via the real `tailwindcss` v3 CLI to compile to `16px`/`24px`/`8px` (bootstrap's own real values, not Tailwind's mismatched `12px`/`16px`/`8px` defaults), while classes outside the bound range (`h-8`–`h-11`, `px-2.5`, `gap-1.5`) were confirmed to still fall back to Tailwind's real, unbroken defaults — proving `extend` (not a full replacement) was the right call. A real `tsc --noEmit` against the same project passed clean. **Disclosed, not fixed:** `bootstrap`'s own keys beyond 5, and RNR's own typography size scale (button/text font sizes) — both real, separate, not-yet-scoped gaps.

**What's left after this pass**: React Native Paper's button padding and typography size scale (a real, separate, not-yet-scoped gap — the same category RNR's typography size is now in, but RN Paper's own radius ceiling, unlike these, is structural and can't be closed further); the `bootstrap` spacing preset's own structural ceiling on shadcn/ui, shadcn-vue, and RNR (all three, independently reached, all disclosed, none fixable without abandoning the "nearest real token" discipline this project has held everywhere else). Every other geometry-fidelity gap flagged in the 2026-09-15 sessions above is now closed. See the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the current, exact disclosure wording — updated alongside this fix, not left to drift.

### Follow-up, same day (2026-09-16) — reopening RN Paper's "structural ceiling," for real

The write-up above called RN Paper's radius gap (every component but Button mismatched) a genuine architectural ceiling — Paper's own `roundness` is a single global value, and there's no per-component override the theme itself exposes. That much was, and still is, true. But the user pushed back with a sharp, correct instinct: if `roundness` alone can't get there, can this pipeline override each component's actual rendered style directly instead of going through the theme — the same way a real app team would end up doing by hand anyway?

**Checked rather than assumed the answer was still "no."** Read the real npm-published `react-native-paper@5.15.3` tarball's own source for Button, Card, Chip, Dialog, TextInput (flat AND outlined), Snackbar, Menu, FAB, ToggleButton, DrawerItem, and Searchbar. Real finding: **most of Paper's own components already have a real, public, documented per-instance override for exactly this** — not a private/unexported internal, not an unsupported hack:
- Button, Card, Chip, FAB: each flattens its own `style` prop, extracts any `border*Radius` key, and uses that INSTEAD of the computed `multiplier × roundness` default (Card even forwards the resolved value to `Card.Content`/`Card.Cover` via `cloneElement`, so one override on the parent stays consistent across the whole card).
- Dialog, Snackbar, ToggleButton, DrawerItem, Searchbar: append the caller's `style` LAST in an array after the computed default — same net effect (React Native's array-style merge means the last value for a shared key wins), just without the extra extraction step.
- Menu: has its own dedicated `contentStyle` prop, appended last the same way.
- TextInput: flat mode's top-corner radius comes from `viewStyle` (everything in `style` except a few text-specific keys), which wins the same way; outlined mode has a dedicated `outlineStyle` prop, Paper's own JSDoc literally says "override the default style of outlined wrapper."

**Two real, confirmed exceptions, not just unchecked**: Tooltip's own `Props` type has no `style` or equivalent prop at all — genuinely not customizable this way. SegmentedButtons computes each segment's radius specially (rounded ends, square middle, via its own `getSegmentedButtonBorderRadius` helper) with no visible override merge in the file — didn't assume the general pattern extends there.

**Built, scoped to Button (already exact) + Card, Chip, TextInput — the three explicitly agreed as the starting set, not all ~15 components at once.** `generate/rn-paper.ts` now also writes `rn-paper/components/Card.tsx`/`Chip.tsx`/`TextInput.tsx` — thin wrapper functions that import the real Paper component and merge `radius.md` into its own real override prop, still overridable further by whatever `style`/`outlineStyle` a caller passes on top. `scaffold/react-native-paper.ts` copies this `components/` directory into the real scaffolded project. `generate/project-docs.ts`'s `AGENTS.md` rule for `hasRnPaper` now tells an AI coding agent to import these three from `./components/`, not directly from `react-native-paper` (which would silently skip the fix and go back to Paper's own mismatched default).

**A real TypeScript-only snag, found and fixed via an actual `tsc` run, not assumed away**: an initial `{ style, ...rest }` destructure-then-spread implementation for the `Card` wrapper failed to typecheck. Isolated the cause with a minimal test: even a totally unmodified `<PaperCard {...props} />` passthrough — zero changes at all — fails the exact same way. This confirmed the problem is a structural limitation of Paper's OWN exported `CardProps` type (a discriminated union keyed on `mode`, each variant a differently-shaped `Omit<..., "ref">`) that TypeScript can't verify through ANY spread of a value typed as that union, not a bug introduced by this wrapper. Fixed with a narrowly-scoped, well-commented `as any` at the one spread — the real runtime object is still a valid `CardProps`, this only works around a real TS-checker limitation. `Chip`/`TextInput`'s own exported types aren't discriminated unions this way and needed no cast, confirmed by the same clean-typecheck test.

**Verified end-to-end with a real scaffolded project**: a real `generate --rn-paper` → `scaffold --rn-paper` run, then a real `tsc --noEmit` — clean, including a realistic usage file exercising `Card` with `Card.Title`/`Card.Content`/`Card.Actions`, a `Chip` nested inside `Card.Actions`, and both `TextInput` modes (`flat` and `outlined`) through the wrappers. The full `md3`/`md2` QA matrix slice (14 real seed cases, 294 checks) still passes with zero regressions.

**Real, disclosed limitation of the wrappers themselves**: they don't forward a `ref`, unlike Paper's own `Card`/`TextInput` (both internally `forwardRef`) — kept deliberately simple rather than re-implementing ref-forwarding for a case most app code doesn't need.

**This changes how RN Paper should be described going forward**: it's no longer accurate to call the geometry gap a flat, unqualified "structural ceiling" — that framing still holds for the SINGLE `roundness` value considered alone, but this pipeline now has a real, verified way around it for four of Paper's components, with six more (Snackbar, Menu, FAB, ToggleButton, DrawerItem, Searchbar) confirmed to follow the same pattern and buildable the same way, and two (Tooltip, SegmentedButtons) confirmed as genuine exceptions. See the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the current, exact wording — updated alongside this fix.

### Follow-up, same day (2026-09-16) — finishing the RN Paper wrapper set

The user asked directly to finish what the write-up above left as "checked but not yet built": Snackbar, Menu, FAB, ToggleButton, DrawerItem, and Searchbar. Dialog was added to the same batch too — it had actually already been fully researched in the very first round (alongside Button/Card/Chip), just not listed among the components originally scoped to build, an oversight caught and corrected while writing this round's own disclosure text rather than left standing.

**Real defaults confirmed for each, against the same real `react-native-paper@5.15.3` source**:
- Snackbar, Menu, ToggleButton: flat `1 × roundness`, unconditional (no `isV3` branch).
- DrawerItem, Dialog: `(isV3 ? 7 : 1) × roundness` — the same multiplier, confirmed independently for each rather than assumed to match just because the numbers happened to line up.
- FAB: genuinely different in *shape*, not just multiplier — real default varies BY SIZE (`getFabStyle` in `FAB/utils.ts`: small `3×`, medium `4×`, large `7×`), plus a wholly separate formula when `customSize` is set.
- Searchbar: also mode-dependent for MD3 — `mode="bar"` (its own real default) is `7×`, `mode="view"` is a deliberate `0` (square by design, not a bug to "fix").

**Override mechanism confirmed for each**: Snackbar/ToggleButton/DrawerItem/Dialog all append the caller's `style` last in their own internal style array (same net effect as Card/Chip/Button's own `splitStyles`-based extraction — the caller's value wins for whatever key it sets, via React Native's real array-style merge, not a JS object spread). Menu has a dedicated `contentStyle` prop instead of `style`, same last-appended treatment. FAB destructures `borderRadius` off the flattened `style` prop with its own computed default as fallback, same shape as Chip.

**A real, functional bug found and fixed before shipping, not a style nit.** While building the Menu/ToggleButton/FAB wrappers, checked (rather than assumed) whether any of the six had real compound sub-components the way Card does (`Card.Content`, etc.) — three did, and the first draft of each wrapper omitted them entirely:
- `Menu.Item` — a real static property on Paper's own `Menu` export (`Menu.Item = MenuItem` in `Menu.tsx`).
- `ToggleButton.Group` and `ToggleButton.Row` — attached via `Object.assign` in `ToggleButton`'s own barrel `index.ts`, not inside `ToggleButton.tsx` itself (would have been missed by only reading the main component file).
- `FAB.Group` (the real speed-dial variant) — same `Object.assign`-in-barrel pattern as ToggleButton.

Left as originally written, importing `Menu`/`ToggleButton`/`FAB` from this pipeline's own wrappers would have made `<Menu.Item>`/`<ToggleButton.Row>`/`<FAB.Group>` disappear entirely for any real app using them — a real runtime break, not a cosmetic gap. Fixed by re-exporting each compound piece as-is on the wrapper function, same treatment Card's own wrapper (round one) already used. Confirmed Snackbar, Searchbar, DrawerItem, Chip, and TextInput have no compound sub-components of their own (checked each one's real barrel file, not assumed from the pattern holding for three others) — no equivalent fix needed there.

**`ToggleButton.Row`'s own real grouping behavior checked for a conflict, none found**: `ToggleButtonRow.tsx` clones each child `ToggleButton` element and injects `style: [button, first/middle/last, child.props.style]` — the `first`/`last` styles zero out only the two INNER corners (`borderTopRightRadius`/`borderBottomRightRadius` for the first item, the mirror for the last). Read through React Native's real style-flattening behavior to confirm this pipeline's own uniform `{ borderRadius: RADIUS }` default and the Row's own longhand corner overrides coexist correctly as separate keys in the final flattened style object (longhand wins for the two corners it targets; the shorthand still applies to the other two) — not a conflict, confirmed rather than assumed compatible.

**Real, disclosed scope limits carried into this round, same restraint as the MUI button-padding fix (medium/contained only) from earlier the same day**: FAB and Searchbar only bind their real DEFAULT case (`size="medium"`, `mode="bar"`) — inventing a scaling formula to preserve Paper's own small/medium/large or bar/view proportions would mean fabricating a ratio this pipeline has no real source for, so `small`/`large`/`customSize` FABs and `view`-mode Searchbars keep Paper's own real, unmodified defaults.

**Verified end-to-end**: a real `generate --rn-paper` → `scaffold --rn-paper` run produced all ten wrapper files; a real `tsc --noEmit` against the scaffolded project passed clean, including a single realistic usage file exercising every one of the ten components and every compound sub-component found above (`Card.Title`/`.Content`/`.Actions`, nested `Chip`, both `TextInput` modes, `Snackbar`, `Menu`/`Menu.Item`, `ToggleButton.Row`/`.Group`, `DrawerItem`, `FAB` default + small sizes + `FAB.Group`, `Searchbar` both modes, `Dialog`/`.Title`/`.Content`/`.Actions`). One real error surfaced during this test — a missing required `visible` prop on `FAB.Group` — and was confirmed to be a mistake in the test file itself, not the wrapper, since `FAB.Group`'s own real prop-checking (preserved through the plain re-export) caught it correctly. The full `md3`/`md2` QA matrix slice (14 cases, 294 checks) re-ran clean with zero regressions after this round too.

**State after this round**: eleven of Paper's real components now have exact, verified radius fidelity (Button via `roundness` alone; ten via wrapper). Two (Tooltip, SegmentedButtons) are confirmed, permanent exceptions. Everything else Paper ships (Badge, Avatar, List items, and more) hasn't been checked — real, separate, not-yet-scoped work, not assumed either way. Button padding and the typography size scale remain fully unbound for every Paper component, unrelated to this round's work. See the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the current, exact wording — updated alongside this fix.

### Follow-up, same day (2026-09-16) — closing RN Paper's LAST disclosed gap: button padding and typography size

Asked directly to solve the remaining RN Paper gap (button padding, typography size scale — the same shape of gap MUI/Vuetify had, closed earlier the same day). Researched both before writing any code, same discipline as every fix this session.

**Button padding — a real, different shape than Bootstrap/MUI/Vuetify's own literal `padding`.** Checked Button's real source rather than assume it has a `padding` property the way the other three libraries do — it doesn't. Paper's `Button.tsx` creates its visual spacing entirely via `marginVertical`/`marginHorizontal` on the LABEL TEXT (`styles.label`/`styles.md3Label`), not the touchable container. Real MD3 default for every mode EXCEPT `"text"` is `marginVertical: 10, marginHorizontal: 24`; `mode="text"` — Button's own actual default when `mode` is omitted — gets a genuinely different, smaller margin with no vertical override at all, a deliberate Material Design distinction (text-mode buttons read as lower-emphasis), not a value to flatten into the others. Confirmed a real, public override path exists for this: `labelStyle`, appended absolute last in the label `Text`'s own style array (`[styles.label, ..., textStyle, labelStyle]`) — the same "caller wins" mechanism already found for the ten radius wrappers. Built as a twelfth wrapper, `rn-paper/components/Button.tsx`, binding `marginVertical`/`marginHorizontal` to the nearest real spacing tokens, applied only for non-`"text"` modes — same restraint as FAB/Searchbar's own mode/size-scoped fixes earlier the same day. Verified `Button`'s own exported `Props` type is a plain intersection, not a discriminated union like `CardProps` — confirmed with a real `tsc` run, no `as any` cast needed here.

**Typography size — genuinely simpler than radius, a real theme-level fix, no wrapper needed.** Checked whether Paper exposes `theme.fonts` as a plain, overridable theme key the same way `colors`/`roundness` already are — confirmed against Paper's real `MD3LightTheme`/`MD3DarkTheme` source (`fonts: configureFonts()`): yes, exactly that simple. Read the real npm-published tarball's own `styles/themes/v3/tokens.tsx` for the actual MD3 typescale — 15 real roles (`displayLarge` through `bodySmall`), identical real px values to this pipeline's own Vuetify `VUETIFY_TYPE_SCALE_DEFAULT_PX` (both reading the same real Material Design 3 spec, not a coincidence). `generate/rn-paper.ts`'s `theme.ts` output now spreads each role's own real default object and overrides just `fontSize` with the nearest real type-scale token — same "size only, leave family/weight/lineHeight/letterSpacing at the library's real default" restraint as this pipeline's own MUI/Vuetify typography-size fixes. Font FAMILY stays unbound, unrelated to this fix — the already-known, separate gap shared by every RN scaffold (real font FILES need `expo-font`/.ttf/.otf, which this pipeline doesn't fetch for React Native at all).

**A small refactor along the way, not scope creep**: the `nearestSpacingPx`/`nearestFontSizePx` helpers first built inside `generate/bootstrap.ts` (2026-09-15/16) were moved into `read-tokens.ts` as a shared `nearestDimensionPx` once `generate/rn-paper.ts` needed the identical "nearest real DTCG token" logic for both the Button margin fix and the typography fix — same "extract once a second real consumer needs it" precedent already set by `shared/scaffold-common.ts`'s own `readCssScale`/`nearestScalePx` (built when MUI/Vuetify needed the CSS-scale equivalent). `bootstrap.ts`'s own call sites were updated to the shared name; no behavior change, re-verified with the real `bootstrap` QA matrix slice (93 checks) to confirm.

**Verified end-to-end**: a real `generate --rn-paper` → `scaffold --rn-paper` run produced the new `Button.tsx` wrapper and the updated `theme.ts`; a real `tsc --noEmit` against the scaffolded project passed clean, including a usage file exercising `Button` across `contained`/`outlined`/`text`/default modes plus a custom `labelStyle` override (confirming the override still wins). The full `md3`/`md2` QA matrix slice (294 checks) re-ran clean with zero regressions.

**State after this round — Layer 2's "installed and wired" libraries are now uniformly closed on this dimension.** React-Bootstrap/bootstrap-vue-next, MUI, Vuetify, and now React Native Paper all have color, radius, button padding, and typography size fully real-token-bound, each via whatever real mechanism that library's own API actually supports (Sass variables, `styleOverrides`, Sass `map-deep-merge`, or — Paper's own case — a mix of generated wrapper components for padding/radius and a direct theme-key override for typography). The only Layer 2 gaps left of this general shape are RNR's own typography size scale (never attempted) and the structural ceilings already disclosed (the `bootstrap` spacing preset's scope limit on shadcn/ui, shadcn-vue, and RNR; Paper's own Tooltip/SegmentedButtons). See the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the current, exact wording — updated alongside this fix.

### Follow-up, same day (2026-09-16) — closing RNR's own typography size scale

Asked directly to solve RNR's remaining typography size gap, right after RN Paper's equivalent fix. Checked whether it was real before writing any code, same discipline as every fix this session: vendored a real RNR project (`--rnr`, real `add -a --styling-library nativewind` run) and grepped its own real `components/ui/` source for Tailwind's named `text-*` classes — confirmed real and meaningful, not theoretical: 36 uses of `text-sm` alone, plus `text-xs`/`text-lg`/`text-base`/`text-xl`/`text-2xl`/`text-3xl`/`text-4xl` across the vendored files. Confirmed `tailwind.config.js` had no `fontSize` key at all before this fix — the gap was real.

**Fetched Tailwind's real default `theme.fontSize` from the actual npm-published `tailwindcss@^3` package** (`stubs/config.full.js`) rather than trust memory: exactly 13 real named keys (`xs`, `sm`, `base`, `lg`, `xl`, `2xl`, `3xl`, `4xl`, `5xl`, `6xl`, `7xl`, `8xl`, `9xl`), real px targets 12/14/16/18/20/24/30/36/48/60/72/96/128 (at the same 16px root this pipeline uses everywhere). A real, useful difference from the earlier spacing fix, found by checking rather than assuming the same shape: Tailwind's fontSize scale has NO keys beyond these 13 (no fractional `text-2.5` the way `px-2.5` exists for spacing) — so extending `theme.fontSize` with all 13 keys is a COMPLETE fix, not a partial one the way spacing's `bootstrap`-preset gap beyond key 5 is.

**Implementation**: `scaffold/react-native.ts`'s `writeTailwindConfig` now also extends `theme.fontSize`, binding each of the 13 real Tailwind keys to the nearest real type-scale token for that key's own real default px target — reusing the already-shared `readCssScale`/`nearestScalePx` helpers from `shared/scaffold-common.ts` (the same ones `scaffold/nextjs-mui.ts`/`scaffold/vuejs-vuetify.ts` already use), no new helper needed.

**Verified three ways, all with real builds**: (1) a real `tailwindcss` v3 CLI compile against the `tailwind` type-scale preset showed a correct no-op — `text-xs` through `text-3xl` and `text-5xl` compiled to exactly Tailwind's own real defaults, since that preset was deliberately built to match Tailwind's scale (same "looks token-driven by coincidence" shape the linear spacing presets and the original Tailwind v4 shadcn bug both had — checked rather than assumed harmless); (2) a real compile against the `md3` type-scale preset showed genuinely different, correct values — `text-sm` → `12px` (not Tailwind's `14px` default), `text-lg` → `19px`, `text-3xl` → `28px` — proving the fix does real work; (3) a real `tsc --noEmit` against both real scaffolded projects, both clean. The real `tailwind`-language QA matrix slice (11 cases, 536 checks) re-ran clean with zero regressions.

**Disclosed, not a bug**: this pipeline's own curated 8-value type-scale mapped onto Tailwind's 13 real keys means several of the larger keys collide onto the same nearest value in the real compiled output — confirmed live, `text-4xl` matched `text-3xl` exactly for the `tailwind` preset, and `text-6xl` through `text-9xl` all matched `text-5xl` (the largest token in the whole 8-value scale, with nothing larger to get closer to). Same class of honest nearest-match collision as MUI's own `h1`/`h3` typography fix earlier the same day — not something to fabricate extra tokens to avoid.

**With this, every Layer 2 "typography size scale" gap flagged this session is now closed** — Bootstrap-family, MUI, Vuetify, RN Paper, and now RNR. The only remaining Layer 2 gaps of any kind are the already-disclosed structural ceilings (the `bootstrap` spacing preset's scope limit on shadcn/ui, shadcn-vue, and RNR beyond their own defined key ranges; RN Paper's Tooltip/SegmentedButtons) and RN Paper's own not-yet-checked components beyond the eleven already covered. See the `SDSGT-start` skill's own "Known gaps," "Geometry fidelity, by library," for the current, exact wording — updated alongside this fix.

### Follow-up, same day (2026-09-16) — confirming RN Paper's radius work is complete, not partial

Asked to continue with whatever was left of Layer 2. Rather than assume RN Paper's "not-yet-checked components beyond the eleven" line above meant real, open work, checked first: grepped the real npm-published `react-native-paper@5.15.3` tarball's ENTIRE component source tree (every file, not just `.tsx` — a first pass missed `FAB/utils.ts` for exactly this reason, `.ts` not `.tsx`, caught and corrected before drawing a conclusion) for every reference to `theme.roundness`.

**Result: exactly 13 real components reference `roundness` anywhere in Paper's source, and the eleven already wrapped plus the two already-confirmed exceptions (Tooltip, SegmentedButtons) account for all 13.** There is no fourteenth component quietly using `roundness` that got missed. Spot-checked the three most plausible remaining candidates directly rather than trust the grep alone: `IconButton`, `Badge`, and `Avatar` (`AvatarIcon`/`AvatarImage`/`AvatarText`) — all compute `borderRadius` as a fixed `size / 2`, a deliberate, always-circular shape per Material Design's own convention, with zero reference to `roundness` or any token — confirmed this is correct, intentional behavior to leave alone, not a gap that happened to be missed.

**Conclusion: RN Paper's radius-fidelity work is complete, not partial.** The "not-yet-scoped" framing in the write-ups above was accurate caution at the time (checked-as-buildable but not exhaustively verified), but turned out to describe an empty set once actually checked. Updated the `SDSGT-start` skill, `contracts-and-seeds.md`, the generated `AGENTS.md` rule, and the scaffold's own README text to say this precisely — "confirmed exhaustively," not "not yet checked" — so a future session doesn't re-open this as if it were still real, open work.

**State of Layer 2 after this check**: nothing buildable remains. What's left is only the two already-disclosed, permanent structural ceilings — the `bootstrap` spacing preset's scope limit (shadcn/ui, shadcn-vue, RNR) and RN Paper's Tooltip/SegmentedButtons (no override path exists in either) — neither fixable without fabricating values or reaching into unexported internals, which this project has consistently declined to do everywhere else. Every other geometry-fidelity gap raised this session, across every library this pipeline supports, is closed.

### Follow-up, same day (2026-09-16) — a full "check everything in Layer 2" pass finds and closes real shadow/opacity gaps beyond geometry

Asked directly to check ALL of Layer 2 for gaps, not just radius/spacing/typography (the dimension this session had been focused on exclusively). Real, honest answer at the start: shadow and opacity had never been audited against any component library at all, this session or before — a real gap in the audit's own coverage, not just in the generated output.

**Real, fixable gaps found and closed, each verified with a real build or running app**:

- **Bootstrap-family**: `$box-shadow-sm`/`$box-shadow`/`$box-shadow-lg` (Bootstrap's own real 3-tier scale — no 4th/5th tier exists) and `$btn-disabled-opacity` (real default `.65`). Both bound to the nearest real token, same discipline as every other Bootstrap fix this session. Verified with a real `next build`'s compiled CSS.
- **shadcn/ui + shadcn-vue (web)**: `--shadow-*` CSS variables. **A real, load-bearing mistake caught by an actual failed browser check, not assumed to work**: the first implementation mirrored the `--spacing` fix's own mechanism (a plain, unlayered `:root` override after Tailwind's own `@theme`) — and it compiled without error, looked correct in the generated CSS text, and STILL had zero real effect. A real browser's `getComputedStyle` showed Tailwind's own unmodified default, not this pipeline's value. Traced to a genuine, non-obvious difference: Tailwind v4 BAKES shadow utility lengths as literal numbers into the compiled CSS at build time (confirmed by reading the actual compiled `.shadow-md` rule — only the color stays a live `var()` reference), unlike `--spacing` (referenced live via `calc()`) or colors/radius (referenced live via `@theme inline`'s own indirection). The real fix had to move the override into an actual `@theme { }` block, which Tailwind's own build step processes directly. Re-verified afterward with two genuinely different shadow presets (not just the one that coincidentally matches Tailwind's own defaults) and got the correct, different real values both times. A second real finding along the way: Tailwind v4 reshuffled its own shadow scale relative to v3 (added a `2xs` tier, shifted names — v3's `sm` is v4's `xs`) — this pipeline's own tokens were built matching v3, so the binding maps `sm` → `--shadow-xs`, not `--shadow-sm`.
- **MUI**: `theme.palette.action.{hoverOpacity,selectedOpacity,disabledOpacity,focusOpacity,activatedOpacity}`, bound to MUI's own real LIGHT-mode defaults (confirmed real light vs. dark values differ; light used because `generate/md2.ts` never sets `mode`, so MUI's own real `mode: 'light'` default silently applies regardless of the seed's light/dark choice — an existing, separately-disclosed limitation, not something this fix changes). Verified live: hovering a real `text`-variant Button in a real browser showed the exact bound alpha channel in its computed `background-color`, not just a successful build.
- **Vuetify**: `theme.variables`'s 11 real opacity/emphasis constants, with genuinely different real light vs. dark defaults (e.g. real `disabled-opacity` is 0.38 light / 0.50 dark). A real, load-bearing verification-methodology finding: these values are injected at RUNTIME by Vuetify's own JavaScript (`genCssVariables`/`upsertStyles`), NOT present in any statically compiled CSS bundle at all — a first attempt at verification via `grep`-ing the compiled `dist/` output found nothing, which could have been mistaken for a failed fix. Correctly verified instead with a real running app: `getComputedStyle(document.documentElement).getPropertyValue('--v-hover-opacity')` showed the exact bound value.
- **RNR**: confirmed real Tailwind v3 defaults for opacity AND border-width already exactly match this pipeline's own tokens at every key either preset defines (checked directly against the real npm-published `tailwindcss@^3` package's own default config) — genuinely no fix needed, not silently assumed fine.

**Real, disclosed, structural gaps found — checked, not fabricated a mapping for**:

- **MUI's `theme.shadows`** is a fixed 25-level array modeling REAL Material Design elevation (a physical depth concept) — different real components use different specific indices for structural reasons (Card=1, AppBar=4, Dialog=24, ...), not a "pick a shadow size" choice the way `sm`/`md`/`lg` are. Mapping this pipeline's own curated 5-tier `shadow.json` onto 25 numeric levels would mean inventing a correspondence with no real source — checked whether one exists in MUI's own docs/spec (it doesn't, in the sense of a canonical "these 5 named sizes = these 5 elevation indices" mapping), and declined to invent one rather than fabricate.
- **Vuetify's own elevation system** (`$shadow-key`/`$shadow-ambient` Sass maps, a real two-layer depth model matching MD3's own spec) — same reasoning as MUI's, a genuinely different kind of scale, not bound.
- **RNR's own shadow classes on native** — checked the actual underlying library (`react-native-css-interop@0.2.7`, the exact version NativeWind v4 pins, confirmed via its own `package.json`) rather than assume binding `theme.boxShadow` would help. Its real `parseBoxShadow` function rejects any box-shadow with more than one layer outright (`options.addValueWarning("multiple box shadows")`, then drops it), and even single-layer shadows only carry over `shadowColor`/`shadowRadius` — no offset, no opacity, no Android `elevation`. This pipeline's own `md`/`lg`/`xl` shadow tokens are real multi-layer definitions (matching Tailwind's own real multi-layer defaults), so they'd be silently dropped by NativeWind regardless of whether this pipeline binds `theme.boxShadow` or not — a structural limitation of the native CSS-to-shadow translation library itself, not something token binding can fix. Not attempted, disclosed instead.
- **RN Paper's own elevation** was checked and confirmed to be a non-issue, not overlooked: it's already handled correctly via real HCT-computed surface-color tinting at fixed dp levels (`generate/rn-paper.ts`'s own `surfaceColorAtElevation` reuse, built earlier this project) — the actually-correct MD3 mobile approach, since neither MD3 nor React Native has a `box-shadow` concept at all. Opacity has nothing to bind either, since MD3 design languages never offer an opacity PRESET choice at seed input in the first place.

**A real, useful side-effect refactor**: `shared/scaffold-common.ts`'s own `readCssScale` was generalized to accept an optional `unit` parameter (default `"px"`, the shape every existing real consumer needs) so opacity's own bare-decimal CSS custom properties (`--opacity-50: 0.5;`, no unit suffix) could reuse it rather than need a fourth near-duplicate scale reader.

**Every disclosure surface updated to match — no stale "not yet checked" language left standing**: the `SDSGT-start` skill's own seed-input rows for Opacity (17) and Shadow (18), its "Known gaps" → "Geometry, shadow, and opacity fidelity, by library" (renamed from "Geometry fidelity, by library" to reflect the now-broader scope), and every per-library step-10 blurb, were all updated in the same pass — both the seed-input-time and Generate-stage disclosure surfaces the user explicitly asked to keep in sync, not just the code.

### Follow-up, 2026-09-16 — closing shadcn/ui's own bootstrap-spacing gap, plus a real, unrelated finding caught while verifying it

Asked directly to fix the one item the "still real, disclosed, not fixed" summaries above kept naming: shadcn/ui and shadcn-vue's `bootstrap` spacing preset, left unbound because the single `--spacing` base-multiplier mechanism (2026-09-15) only works for a linear scale, and `bootstrap` (0/4/8/16/24/48px) isn't one.

**Fixed with a second, different mechanism, not a variant of the first.** `generate/tailwind.ts` gained a shared `readSpacingEntries` helper (refactored out of `computeLinearSpacingConstant`, which now calls it too — the same "extract once a second real consumer needs the identical logic" discipline used repeatedly this session). `generate/shadcn.ts`'s `generateShadcn` now uses it: when the resolved preset is non-linear, write a NAMED `--spacing-<N>` override for each key `spacing.json` itself actually defines (0-5 for bootstrap) into a real `@theme { }` block — the SAME block the existing `--shadow-*` fix already uses, for the same underlying reason: Tailwind only recognizes a new `--spacing-<N>` as a utility-generating theme key when it's declared inside a real `@theme` block its own build step scans; a plain `:root` declaration (right mechanism for the linear case's single constant) is inert here, since Tailwind's compiled `.p-4` rule falls back to `calc(var(--spacing) * 4)` regardless unless `--spacing-4` was visible at build time.

**A real, pre-existing documentation bug caught and fixed along the way, not introduced by this change**: `generate/shadcn.ts`'s own file-header comment for the `--shadow-*` fix claimed it shared "the same injection point/mechanism as `--spacing` above (this file's `:root`, unlayered...)" — but the actual code, and that same header's own later shadow-specific paragraph, correctly describe a real `@theme` block, not `:root`. The two paragraphs contradicted each other; corrected the wrong one while documenting the new fix in the same place, rather than let a second inconsistency sit next to the first.

**Verified with a real build, not just inspected**: a fresh `promote` → `generate --shadcn` with `spacingPreset: "bootstrap"` produced a `shadcn/theme.css` with no `--spacing` constant in `:root` (correctly absent — the linear branch didn't fire) and the expected `--spacing-0` through `--spacing-5` in a real `@theme` block. A full `scaffold --shadcn` + `next build` showed the compiled rule `.px-4{padding-inline:var(--spacing-4)}` resolving to `24px` — bootstrap's own real value, genuinely different from Tailwind's own `16px` default for that key, not a coincidental match. A live Playwright check on a rendered Badge (`px-2`) confirmed `8px` computed padding, though that key happens to coincide with Tailwind's own default either way — the `px-4` compiled-CSS check is the non-coincidental proof. Re-ran the full QA matrix scoped to the shadcn platform across every core/achromatic/mismatch/baseline/boundary case (30 cases, 1560 checks) — all passed, no regressions to the three already-working linear presets.

**Disclosed, not fixed, same shape as before**: any class using a spacing key beyond what the preset defines (`h-8`, `gap-6`+), or any fractional class (`px-2.5`, `gap-1.5`), still falls back to Tailwind's raw default — this fix extends the bound range, it doesn't recreate Tailwind's full ~30-key scale from a non-linear preset (which has no defined value for most of those keys in the first place).

**A claim made and then retracted the same day, worth recording so it isn't repeated:** rendering the real vendored `Button` to test this fix showed its default size as `h-8 gap-1.5 px-2.5` — matching what `figma-components-plan.ts`'s `buildButtonPlan` and this project's own docs already assumed (confirmed via `git log -p` on that file: its geometry constants, `radius=8`/`paddingH=10`/`paddingV=5`/`fontSize=14`, have targeted exactly `rounded-lg`/`px-2.5`/`text-sm` since the very first commit that introduced the file — never `h-9 px-4 py-2`). A first pass at writing this up claimed shadcn's real Button source had "drifted" away from an `h-9 px-4 py-2` baseline this project supposedly recorded — that baseline never existed; the claim was made without actually opening `figma-components-plan.ts` or checking its git history first, and was wrong. Corrected immediately once the user asked to act on it and the file was actually checked. No real drift, no fix needed — Button's already-shipped Figma-push geometry and this bootstrap-spacing fix are both still accurate against real current shadcn/ui source.

Updated the `SDSGT-start` skill's shadcn/ui bullet (in "Geometry, shadow, and opacity fidelity, by library") and `contracts-and-seeds.md`'s own shadcn/ui theming section to reflect the fix — the false drift claim briefly added to both was removed once caught.

## New scope addition — icons as assets

**Partially already flagged** (`pipeline-plan.md`, "Deferred": *"Fonts, icons, logos as actual files (not just a token naming a font family) — a real asset-delivery pipeline is out of scope for now, to be designed when it's actually needed."*) — but it's been sitting there as a vague, bundled-with-fonts-and-logos placeholder. **It becomes concretely relevant, not just theoretically relevant, the moment Subject 2 starts**, because real vendored components commonly import icon components directly, and every one of these libraries has its own icon convention:

- **shadcn/ui — resolved for free, confirmed 2026-09-12.** `shadcn init`'s own `-d` default preset already picks `lucide-react` and adds it as a real dependency itself, with no action needed from this pipeline — the real `package.json` from a scaffolded run shows `lucide-react` installed automatically.
- **Vuetify — resolved for free, confirmed 2026-09-14.** `create-vuetify`'s own default preset vendors `@mdi/font` (Material Design Icons) automatically — see Subject 4's own real scaffold run.
- **React Native Paper — resolved for free, confirmed 2026-09-14.** Expo bundles vector icons already; no extra install needed — see Subject 5.
- **bootstrap-vue-next on Vue.js — resolved, confirmed 2026-09-14 (same day as the scaffold itself).** `bootstrap-icons` (the official Bootstrap team package, zero runtime dependencies, confirmed via its real `package.json`) now installs alongside `bootstrap`/`bootstrap-vue-next` and gets imported in `main.ts`; the proof-of-tokens page uses a real `bi bi-check-circle-fill` icon so it's demonstrably wired — verified in the real compiled `dist/` output (the icon's CSS rule plus its fingerprinted `.woff2`/`.woff` font files). Deliberately not `bootstrap-icons-vue` (a third-party per-icon Vue-component wrapper, not maintained by the Bootstrap team) — plain `bi bi-*` classes match how every other Bootstrap consumer already uses icons.
- **React-Bootstrap on Next.js — resolved, added right after the Vue.js path above.** `bootstrap-icons` installs alongside `bootstrap`/`react-bootstrap` and gets imported in `layout.tsx`; the proof-of-tokens page uses a real `bi bi-check-circle-fill` icon — verified in the real compiled `.next/static` output (icon rule plus fingerprinted font files), same bar as the Vue.js path.
- **MUI on Next.js — resolved, added right after.** `@mui/icons-material` (official MUI package, peer-pinned to the exact `@mui/material` version) installs alongside it; the proof-of-tokens page uses a real `<CheckCircleIcon />`. **With this, every component library this pipeline has ever built a real scaffold for now has its icon story resolved — nothing left open here.**

## New scope addition — the pull half of token sync (2026-09-17)

Asked directly to "keep me honest" about whether editing tokens in Figma, or in code, actually flows through to the components — the honest answer at the time was no on the Figma side: `SDSGT-figma-push` only ever pushes (one-time, first-push-only), and `pipeline-plan.md`'s own "Token sync" concept (edit a value in Figma or code, sync just that change back) was a stated architecture goal, never built — confirmed by grepping the real CLI for a `sync` command (none existed) and by `pipeline-plan.md`'s own "Still open" list explicitly naming the sync engine's packaging as unresolved.

Asked to build the pull direction. One real design decision up front, asked directly rather than assumed: how does a pull step know a Figma value was actually *edited*, versus just reading its current (unchanged) value? Chose to keep a snapshot and diff precisely, over the simpler "always overwrite everything from Figma" — the latter would silently overwrite any token only ever touched in code, and gives no visibility into what actually changed.

**Built**: `cli/src/promote/figma-pull.ts` (`computeFigmaPull`, `writeInitialSnapshot`), wired as a new `figma-pull` CLI command (`--tokens-dir`, `--live-variables`, `--init`). Mechanism:

- A snapshot (`figma-sync-snapshot.json`, alongside the DTCG spec) records every pushed variable's fully-resolved value at the time of the last push or pull. `SDSGT-figma-push`'s step 4 now writes this right after variables land (`--init` mode — no diffing, just establishes the baseline from Figma's own real resolved values, not the push plan's placeholder literals for alias/alpha entries).
- A later pull (`SDSGT-figma-push`'s new step 8) reads live variable values (same `figma_get_variables({ resolveAliases: true })` call), diffs against the snapshot, and writes only the tokens that changed straight into the real DTCG JSON files `generate` reads from.
- **Alias-following, not alias-breaking, by construction**: reuses `figma-plan.ts`'s own `buildFigmaPushPlan` (computed fresh from current DTCG state, not stored) to know which semantic tokens are aliases and what they point to. A semantic token whose live value changed ONLY because its alias target changed is skipped (no JSON write, no double-report) — the primitive's own diff entry covers it, and the alias itself stays intact so `generate` picks up the new value for every semantic token that references it. A semantic token overridden INDEPENDENTLY in Figma (its own live value no longer matches its alias target's) gets converted to a literal, with an explicit note — never silently.
- Value formatting copies whatever convention the EXISTING token already used (a `"<N>px"` dimension string gets a new number wrapped the same way; a bare `number`/`fontWeight`/color hex stays as reported) rather than asserting one from the Figma variable's own type — correct across every real DTCG shape checked (`opacity.json`'s plain numbers, `border-width.json`'s dimension strings, `typography.primitive.json`'s fontFamily/fontWeight, `color.semantic.*.json`'s alias strings).

**A real bug caught in testing, not shipped**: the first version compared an alias's live value against its target using the SAME mode key ("light"/"dark") — but a primitive/foundation target is mode-independent (only ever has a `"value"` key), so every alias looked "broken" simply because the mode names never matched. Fixed by falling back to the target's `"value"` key when the same-mode lookup misses. Caught by writing two real test scenarios (a primitive change that should make its alias silently follow along; a semantic token overridden independently that should convert to a literal) and inspecting the actual written JSON, not just the printed summary — the first run's real output (both scenarios wrongly converting to literals) would have shipped a broken alias-following guarantee if not checked this way.

**Verified**: two real end-to-end scenarios against real token files (color primitive change + alias-following + a spacing dimension change; a direct semantic-token override with the other mode's alias left untouched), the `--init` path, the no-snapshot error path, and the unmapped-variable disclosure path — all inspected via the actual written JSON, not just console output. Full CLI-level QA group re-run clean (5/5) after the `cli.ts` changes.

**Real, disclosed scope — not the full sync engine described in `pipeline-plan.md`, one real piece of it:**

- **Pulled every variable AND style category as of the same-day follow-up below** — see that entry for text styles/shadow effect styles, added right after this first cut shipped variables-only.
- One direction per run, on demand only — never re-pushes code-side changes back to Figma in the same pass, and never auto-detects "did code also change independently" (the snapshot only ever tracks Figma's own last-known state, by design).
- Doesn't chain into `generate` — pulling updates the token SPEC only; refreshing the actual code (`theme.css`/`theme.ts`/component files) is still a separate, explicit `generate` re-run, same promote→generate confirmation gate this pipeline uses everywhere else.
- Only runs from inside this pipeline's own `cli/` checkout today — a generated/scaffolded project has no copy of this CLI to invoke on its own. How that gets packaged for a truly handed-off project is still open (see `pipeline-plan.md`, "Still open").

### Follow-up, same day (2026-09-17) — pull now covers styles too, not just variables

Asked directly, right after the variables-only cut above shipped, to also pull text styles and shadow effect styles — the user explicitly said code-side drift detection doesn't matter to them ("I expect it to be managed from Figma"), simplifying the scope: no need to reconcile "did code change independently," only "what changed in Figma."

**Real complication found before writing any code**: text styles aren't flat values the way variables are — each of a style's four fields (`fontFamily`/`fontWeight`/`fontSize`/`lineHeight`) is its OWN DTCG alias into a typography primitive (`typography.semantic.json`'s real shape: `{"fontSize": "{typography.primitive.fontSize.12}", ...}`), so the exact same alias-following problem already solved for semantic colors applies again, per-field. Extended `FigmaTextStylePlanItem` with an optional `aliasFigmaNames` map (which primitive Figma-variable-name each field currently points to, when it IS an alias) so `figma-pull.ts` can reuse the identical "does this field's live value still match its alias target's live value" check.

**A real, separate bug caught and fixed while building this, not shipped**: `figma-plan.ts`'s own text-style `resolve()` helper unconditionally called `.replace()` on every field, assuming it was always an alias reference string. That was true before this session but stops being true the moment a pull ever converts a field to a literal — the next time `buildFigmaPushPlan` runs (which both push AND pull call fresh, every time), it would crash on a literal fontSize (a number has no `.replace()`) or produce garbage on a literal string (an empty path array once split on `.`). Fixed by short-circuiting `resolve()` (and the new `aliasFigmaName()`) to return early when a field isn't wrapped in `{}` — a real, load-bearing fix, not a defensive-programming nicety, since without it the SECOND pull against a project with any overridden text-style field would have thrown.

**Shadow effect styles needed no alias-following at all** — `shadow.json`'s layer values (offsetX/offsetY/blur/spread/color) were never aliases to any primitive scale to begin with, so a changed layer always just gets written directly. Compared layers with a tolerant numeric check (`layersEqual`/`parseRgba` in `figma-pull.ts`) rather than plain string equality, since a Figma-reported alpha channel could stringify slightly differently than this pipeline's own writer — a real robustness call, not overengineering: naive string diffing here would have produced false-positive "changes" on unchanged shadows purely from formatting noise, undermining the whole point of only reporting real edits.

**Verified with three real end-to-end scenarios**, each inspected via the actual written JSON: (1) a typography primitive change with a text style correctly following along (skipped, no write, no double-report); (2) a text style's `fontSize` overridden independently of its primitive, correctly converted to a literal while its other three fields stayed aliases; (3) a shadow style's blur value changed, correctly rewriting just that layer's `$value` while the untouched layer stayed byte-identical. Also verified an old-format snapshot (missing the new `textStyles`/`effectStyles` keys entirely, simulating a project that pushed before this extension existed) doesn't crash — treated as "nothing to diff against yet" for every style, the same safe default new tokens already get. Full CLI QA group re-run clean (5/5) after the `cli.ts`/`figma-plan.ts` changes.

**`SDSGT-figma-push`'s own mechanics updated to match**: the pull-baseline write moved from the end of step 4 (variables only) to the end of step 5 (once styles exist too), the CLI flag renamed `--live-variables` → `--live-data` (now a `{ variables, textStyles, effectStyles }` object, not a bare array), and step 8 rewritten to describe the full read (including the weight-name reverse-mapping needed to turn a Figma `fontName.style` like `"Medium"` back into this pipeline's own numeric `600`, and the exact `rgba(r,g,b,a)` string format effect-style colors need to match).

**State after this round**: the pull mechanism now covers every variable AND style category this pipeline pushes — nothing pushed is left un-pullable. Still real, disclosed, unchanged from the first cut: one direction only, no code-side drift detection (by explicit user choice this time, not just an unaddressed gap), no auto-chain into `generate`, and only runnable from this repo's own `cli/` checkout.

## New scope addition — closing the "update an already-scaffolded project" gap (2026-09-17)

Asked to close a real, disclosed hole: `figma-pull` and a hand token edit both update the DTCG spec, and `generate` re-running in place refreshes the code-token files (no "already exists" guard on `generate --out`) — but **nothing propagated either kind of edit into an already-SCAFFOLDED project**. Every scaffold entry point (`nextjs.ts`, `nextjs-bootstrap.ts`, `nextjs-mui.ts`, `vuejs.ts`, `vuejs-bootstrap.ts`, `vuejs-vuetify.ts`, `react-native.ts`, `react-native-paper.ts`) unconditionally threw `"${outDir} already exists"` — and every one of those same files' own written comments/README text already claimed "re-run `generate` then `scaffold` to update," a real bug that's been present since scaffold's own existence guard was written: that workflow was never actually possible.

**Design decision, confirmed against the plan before building**: a `--update` flag on the EXISTING `scaffold` command (not a new dedicated command) — same `--framework`/library flags decide which theme files get touched, `--code-dir`/`--out` mean the same thing, just `--out` now must point at an EXISTING project instead of a fresh path. Every platform's own normal write logic is reused as-is (the exact same `copyFileSync`/`writeThemeTs`-shaped functions a fresh scaffold call already uses, most of them renamed from `write*` to `build*` and changed to return a string/Buffer instead of writing directly, so both a fresh scaffold and an `--update` call can share one code path) — `--update` just skips the official-CLI project-creation step, dependency installs, and one-time boilerplate (App.tsx/page.tsx/layout.tsx/README.md, vendored-component installation) that only make sense once, at creation time.

**Three real pieces, all built together since they're interdependent**:

1. **The `--update` pathway itself**, in all eight scaffold files plus `cli.ts`'s `runScaffold`. Guarded via a small `guarded()` closure per file wrapping the new `guardedWriteFile` helper (`shared/scaffold-common.ts`) — writes normally when there's no prior record or the hash matches, warns and skips when it doesn't, unless `--force`.
2. **A real, pre-existing bug fixed in the shadcn/ui and shadcn-vue merge logic** (`appendShadcnThemeOverride` in `nextjs.ts`, `finalizeShadcnMainCss` in `vuejs.ts`): the SDSGT-owned override block had a start comment but no end marker, so a second append (which `--update` now needs to do routinely) would have duplicated the block forever, growing the file every run. Fixed with a real end marker (`SDSGT_BLOCK_END`, `shared/scaffold-common.ts`) and a new `replaceOrAppendSdsgtBlock`/`guardedBlockReplace` pair that finds the block by its start/end markers and replaces it in place. **Verified live**: ran `scaffold --update` against a real Next.js+shadcn project TWICE in a row (with a real token color change in between) — exactly one `SDSGT overrides below`/`SDSGT overrides end` pair in `globals.css` both times, new color present, and a real `next build` succeeded after both runs.
3. **A new hash-based customization guard for token-derived theme files**, `sdsgt-theme-manifest.json` — a deliberate SIBLING to `sdsgt-vendored-components.json`, not an extension of it: the existing manifest is scoped to vendored COMPONENT source (shadcn's/RNR's registry files) and is left completely untouched by this work, per its own established scope; the new one covers `theme.ts`/`theme.css`/`theme-light.css`/`theme-dark.css`/`theme-palette.ts`/`_variables.scss`/`global.css`/`constants.ts`/`settings.scss` and the shadcn(-vue) override block specifically — same "never blindly overwrite a customization" discipline, applied to a different class of file. **Verified live**: hand-appended a comment to a real project's `theme.ts`/`theme.css`, changed a token, ran `--update` — the hand-edited file was left untouched with a plain warning printed, every OTHER (non-hand-edited) file in the same run refreshed correctly, and `--force` afterward overwrote the hand-edited file as expected.

**A second real, pre-existing bug found and fixed while building this, unrelated to the guard/idempotency work above**: `generate --rnr` has always written TWO files into its output — `rnr/global.css` and `rnr/constants.ts` (the `NAV_THEME` object for React Navigation, see `generate/rnr.ts`) — but `scaffold --framework react-native` only ever copied `global.css` into the actual project. `constants.ts` was silently never delivered to any React Native project scaffolded before this fix, on a fresh scaffold OR an update — not something `--update` introduced, a latent gap in the ORIGINAL scaffold path, caught by cross-checking `generate`'s real file list against what `react-native.ts` actually copied rather than assuming the two already agreed. Fixed by copying it (guarded, same as every other theme file) whenever it exists in `codeDir`. **Verified live**: a real `scaffold --framework react-native` run now lists `constants.ts` in its own file-written output and the file lands in the project root with real `NAV_THEME` content; a follow-up `--update` after a token edit correctly refreshed it.

**Scope deliberately left out, with reasons, not silently skipped**:

- **RN Paper's eleven wrapper components** (`components/Button.tsx`, `Card.tsx`, etc.) bake real token values (radius/padding numbers) directly into each file at generate time, same as `theme.ts`, rather than referencing an external CSS-variable file the way vendored shadcn/RNR components do — so these ARE refreshed and individually hash-guarded by `--update`, unlike shadcn's/RNR's actual vendored registry files (Button.tsx aside, which is a real Paper wrapper, not a shadcn-style vendored file — no naming collision, just worth being precise about which "Button" is which).
- ~~A real, disclosed limit on the shadcn-vue path's font-face handling under `--update`~~ — **closed 2026-09-17, see the dated follow-up entry below.**
- ~~A real, disclosed interaction for MUI specifically (a `--fonts-dir` added on a later `--update` never gets imported into `layout.tsx`)~~ — **closed 2026-09-17, see the dated follow-up entry below.**
- **Vendored component SOURCE (shadcn's/RNR's `.tsx` files) is untouched by `--update`**, exactly as scoped going in — they already reference the theme/CSS-variable files this pathway refreshes, so a token change reaches them for free without re-vendoring; the existing `sdsgt-vendored-components.json` guard and re-vendor logic (still not built) are unrelated and unmodified.

**Comments fixed to describe the real, now-true workflow**: every scaffold-file comment and generated-project README line that said "re-run `generate` then `scaffold` to update" or "the next scaffold run overwrites this file" (11 occurrences across all eight scaffold files, more than the roughly-7 originally estimated) now says the real thing — run `generate` then `scaffold --update` (same flags, `--out` pointing back at the existing project).

**Verified overall**: real end-to-end runs covering all three explicitly required scenarios (Next.js+shadcn idempotent block-replace run twice, MUI's `theme.ts`/`theme-palette.ts` overwrite + hash-guard, and the guard actually firing on a hand-edited file before falling back to `--force`), plus React Native/RNR's `constants.ts` fix, each checked via the real written files and/or a real `next build`, not just reading the code. `npx tsc --noEmit` clean throughout. Full `qa:matrix --group=cli` (5/5) and `--group=core` (16/16, 2528 checks) re-run clean after all changes — no regressions in existing promote/generate/scaffold behavior.

### Follow-up, 2026-09-17 — closing the two `--update` font gaps the entry above left open, and a deeper, unrelated font bug found while verifying them

Asked to close the two real, disclosed gaps the "Scope deliberately left out" list above named: shadcn-vue's font-face handling under `--update` (`vuejs.ts`), and MUI's `layout.tsx` never importing a `fonts.css` added on a later `--update` (`nextjs-mui.ts`).

**1. shadcn-vue's font-face `--update` gap.** Diagnosed the real mechanism first, not assumed from the prior entry's own description: `finalizeShadcnMainCss`'s font-face handling was a plain `existingMainCss.includes(fontFaceCss.trim())` substring check with no bounding markers at all — it could tell "this exact block is already present" (skip, avoid a duplicate) but had no way to find and remove a DIFFERENT font's block, since nothing marked where "this pipeline's font-face rules" started or ended in the file. Fixed with the same "guarded block replace" mechanism the theme-override block already used for the identical reason (`scaffold-common.ts`'s `guardedBlockReplace`/`replaceOrAppendSdsgtBlock`/`extractSdsgtBlock`, all generalized to take an explicit `{ start, end }` marker pair — defaulting to the original `SDSGT_BLOCK_START`/`END` so every existing caller is unaffected — once a second real consumer needed the identical logic, same discipline this project already applies elsewhere). A new, independent marker pair (`SDSGT_FONT_BLOCK_START`/`END`) bounds the font-face rules specifically, hash-guarded under its own manifest key (`main.css#sdsgt-font-block`) separate from the theme-override block's own key, so a hand-edit to one never blocks the other. **Verified live**: scaffolded a real Vue.js + shadcn-vue project with Roboto (real `.woff2` files, `cli/seeds/test-project-fonts/`), confirmed a real `vite build`'s compiled CSS had exactly 3 Roboto `@font-face` rules and `--font-sans:Roboto,sans-serif`; changed the seed to Montserrat, re-ran `promote` → `generate` → `scaffold --update`, and confirmed the rebuilt compiled CSS had exactly 2 Montserrat `@font-face` rules (only 400/600 — the fixture has no montserrat-700), zero remaining Roboto `@font-face` rules, and `--font-sans:Montserrat,sans-serif`. Also verified the new font-face block's OWN hash guard: hand-edited a comment into it, changed fonts again, ran `--update` — the font-face block was correctly left untouched with a warning (while the THEME-override block, not hand-edited, updated normally in the same run — the two guards are independent, as designed), then `--force` correctly overwrote it.

**A second, deeper, pre-existing bug found while verifying the above with an actual rendered page, not just compiled CSS text** (the task's own explicit instruction — this is exactly the class of thing a text-presence check misses): a real Playwright `getComputedStyle` check on the rendered page showed `font-family: "Geist Variable", sans-serif` on `body`/`h1` — the SAME on-disk brand font (Roboto, then Montserrat) declared correctly the whole time, but never actually applied to anything. Root cause: shadcn-vue's own `init -d -y` output (current as of 2026-09-17) declares `--font-sans`/`--font-heading` inside an `@theme inline { ... }` block, and Tailwind v4's `inline` keyword means the compiler bakes that LITERAL value directly into every utility class that reads it at BUILD time — confirmed against the real compiled output (`.font-sans{font-family:Geist Variable,sans-serif}`, a literal, not `var(--font-sans)`). This pipeline's own override block, appended AFTER shadcn-vue's defaults on the (previously correct) assumption that a later CSS declaration wins the cascade, has zero effect on already-inlined utility output — a build-time transform, not a runtime lookup, so source order never mattered here. This predates today's session entirely (present since the Vue.js + shadcn-vue path was first built 2026-09-14, on every fresh scaffold too, not just `--update`) and was never caught before because verification up to this point checked compiled CSS TEXT for the right family name (present — just never consumed), never an actual rendered computed style. **Fixed** with `patchShadcnDefaultFontVars` (`vuejs.ts`) — a plain, unguarded, idempotent regex substitution (same "small deterministic edit to shadcn's own generated boilerplate" precedent `stripDefaultGoogleFontsImport` already uses in this file) that patches shadcn-vue's own `--font-sans`/`--font-heading` declarations IN PLACE, wherever they're declared, rather than relying on a competing later declaration — deliberately not scoped to `@theme inline` specifically, so it keeps working if shadcn-vue's template stops using `inline` later. The existing override block's own declaration was kept, not removed (harmless, and correct again if that ever happens). **Verified live, this time with an actual rendered page**: real Playwright `getComputedStyle` checks on both the Roboto and Montserrat states, on both a fresh scaffold and after `--update`, all showing the real brand font (`"Roboto, sans-serif"` / `"Montserrat, sans-serif"`) on `body` and `h1` — not "Geist Variable" — plus `document.fonts` showing this pipeline's own `@font-face` entries actually `loaded`, not a coincidental system-font match. **One precisely-disclosed interaction, not a bug**: this direct patch is unconditional, while the font-face `@font-face` rules themselves stay guarded — if the font-face block is hand-edited and gets skipped on an `--update` that also changes fonts, the declared family name (both here and in the override block) still moves to the NEW font while the actually-available `@font-face` rules stay on the OLD one until the warning is resolved (`--force` or a hand-merge); the printed warning already covers this (`main.css: ... left untouched...`), so it's a visible, actionable state, not a silent one — not tightened further, since keeping two independently-guarded regions in lockstep would be new complexity for an edge-of-an-edge case with no reported real occurrence.

**2. MUI's `layout.tsx` fonts.css import gap.** Confirmed the exact mechanism from the prior entry's description (correct as stated): `hasFonts` becomes true on an `--update` run passing `--fonts-dir` for the first time, `fonts.css` gets written, but `layout.tsx` — never rewritten by `--update`, since a user may have built real UI into it — still has no import for it, and the code previously only printed a warning. Fixed by detecting this exact condition (`fonts.css` needed + `layout.tsx` missing the import) and inserting the ONE missing `import "./fonts.css";` line via a new `injectFontsCssImport` (`nextjs-mui.ts`) — finds the last `import ...;` statement in the top-of-file import block and splices the new line in right after it (works with this pipeline's own template, which has a mid-block blank line, and with a lightly hand-edited file that still starts with real imports), rather than a wholesale `layout.tsx` regeneration. Hash-guarded via the EXISTING `guardedWriteFile`/`sdsgt-theme-manifest.json` mechanism, not a new one: a fresh (non-`--update`) scaffold now also records `layout.tsx`'s own hash into the manifest right after writing it (previously untracked, since nothing needed to touch it again before this fix), giving a real baseline a later `--update` run can hash-check before deciding it's safe to auto-inject. **Verified live**: scaffolded a real Next.js + MUI project with no `--fonts-dir`, confirmed a real `next build` succeeded on the system-font fallback; added real font files and re-ran `promote` → `generate` → `scaffold --update --fonts-dir <dir>`, confirmed `layout.tsx` gained exactly the one new import line (rest of the file byte-identical), a real `next build` succeeded, and a live server + Playwright check showed `getComputedStyle` on the rendered `Typography`/`Button`/`body` all reporting `"Roboto, sans-serif"` (not the default MUI stack) plus `document.fonts` showing this pipeline's own `@font-face` entries `loaded` and the real `.woff2` file served with a `200`. Also verified the hash guard: hand-edited `layout.tsx` (added a real comment) before the same `--update --fonts-dir` run — left untouched with a plain warning, the hand-edit marker intact, no import added; `--force` afterward injected the import while leaving every other hand-edited line in the file untouched (a surgical splice, not a destructive full rewrite) and the project still built.

**Both fixes typechecked (`npx tsc --noEmit`, clean) and re-verified against the full QA matrix — `--group=cli` (5/5) and `--group=core` (16/16, 2528 checks) both clean, no regressions.**

**A note on scope, precisely bounded, not left vague**: the SAME `@theme inline`-defeats-later-overrides shape found in shadcn-vue's Vue.js path was NOT checked against the sibling Next.js + shadcn/ui path (`nextjs.ts`) — genuinely possible if `npx shadcn@latest init`'s current template uses the same `@theme inline` convention (shadcn/ui and shadcn-vue share a common design), but this wasn't verified either way this session, since `nextjs.ts` was explicitly out of scope for this pass (only the two named `vuejs.ts`/`nextjs-mui.ts` gaps were asked for). Also NOT touched: `nextjs.ts`'s own shadcn/ui path never re-appends/refreshes `@font-face` rules AT ALL on `--update` (the font-face append there sits inside its own `if (!update)` guard, unlike shadcn-vue's, which at least attempted a substring-based re-append before this session's fix) — meaning a changed font on an already-scaffolded Next.js + shadcn/ui project currently has NO update path at all, not even the old, partial shadcn-vue-style substring check. Found by inspection while reading `nextjs.ts` for context on this session's own fix, not fixed — flagged here precisely so it isn't lost, since it wasn't in either of the two gaps this session was asked to close.

### Follow-up, 2026-09-17 — checking the flagged Next.js/shadcn font gap: confirmed real, but a genuinely different mechanism than shadcn-vue's, not the same bug in a second file

Asked to check the exact gap the entry above deliberately left open: does `nextjs.ts`'s shadcn/ui path have the same font-rendering bug the Vue.js path had. It does — confirmed live, not assumed from the surface-level code similarity — but via a **different root cause**, which changed the shape of the fix.

**What's actually different from shadcn-vue.** Inspecting a real `shadcn@latest init -d -y` output (current registry, same 61-component vendor as before): shadcn/ui's Next.js template's `@theme inline` block declares `--font-sans: var(--font-sans);` and `--font-heading: var(--font-sans);` — a **self-referential runtime custom-property reference**, not a baked literal the way shadcn-vue's `Geist Variable, sans-serif` was. Confirmed against the real compiled `.next/static` output: `.font-sans{font-family:var(--font-sans)}` — Tailwind v4's `inline` keyword preserves `var()` indirection when the source value is itself a `var()` reference, it doesn't force a literal bake in that case. So, on its own, the OLD append-a-later-`:root`-block approach *should* have worked here (ordinary CSS cascade, not a build-time transform defeating it). It didn't, for a second, previously-unflagged reason: **`shadcn init` itself rewrites `layout.tsx`**, re-adding a `next/font/google` Geist import (`const geist = Geist({subsets:['latin'],variable:'--font-sans'})`) even though `nextjs.ts`'s own `rewriteLayoutTsx` runs first specifically to strip `next/font` (per the file's header comment: "Fonts deliberately do NOT use next/font/google" — a real drift-risk concern, not stylistic). Confirmed by code order (`rewriteLayoutTsx` runs before `runShadcnInit`) and by inspecting the real post-scaffold `layout.tsx`, which had the Geist import back. Geist's own generated CSS module (`.geist_xxx__variable{--font-sans:"Geist","Geist Fallback"}`) sets that property on `<html>` at runtime, at equal specificity to this pipeline's own `:root` rule, and won in practice — confirmed with a real Playwright `getComputedStyle` check on the unfixed scaffold: `body`/`h1`/`html` all reported `"Geist, \"Geist Fallback\""`, not the seed's real fonts (Roboto/Montserrat), even though the correct family names were textually present in the compiled CSS the whole time. A second, related finding: shadcn's own template maps `--font-heading: var(--font-sans);` — an ALIAS of the body-font property, not an independent role — so even setting aside the Geist collision, `--font-heading` was never actually separable from `--font-sans` in the unfixed code.

**Fixed two ways, matching the two real causes, not one fix copy-pasted from `vuejs.ts`:**
1. **`rewriteLayoutTsx` now runs a second time, after `runShadcnInit`/`runShadcnAddAll`**, so shadcn's own reintroduced Geist import doesn't survive into the final project — closes the actual root cause (the runtime `--font-sans` collision) and also removes a real, unwanted Google-Fonts dependency/bundle weight the file's own header comment already said this pipeline avoids.
2. **A new shared helper, `patchFontFamilyVars` (`cli/src/shared/scaffold-common.ts`)** — extracted from the same regex-substitution shape `vuejs.ts`'s own `patchShadcnDefaultFontVars` already uses (that file was left untouched, per this session's scope — the extraction is a NEW consumer in `nextjs.ts` only, not a migration of the existing one), patches shadcn's own declared `--font-sans`/`--font-heading` values in `@theme inline` directly in place, independently of one another (fixing the alias problem too), rather than relying on a later declaration to win the cascade. This is deliberately defense-in-depth on top of fix #1: even if a future shadcn version reintroduces some other mechanism that sets `--font-sans` at runtime, the compiled utility no longer reads `var()` at all once patched, so nothing could re-win. Runs unconditionally on every scaffold AND `--update` (not gated like the shadcn-init block), since a changed font on `--update` needs it reapplied — same "direct patch is unconditional, the guarded theme/color block is separate" interaction already disclosed for `vuejs.ts`, restated in `nextjs.ts`'s own comments.

**Verified live, real build + real render, both before and after:**
- **Before the fix**: real `next build` (Turbopack) succeeded, but Playwright `getComputedStyle` on the running app showed `body`/`h1`/`html` all rendering `"Geist, \"Geist Fallback\""`, `document.fonts` showing 6 real Geist font-face entries (one `loaded`), and `layout.tsx` confirmed to contain the reintroduced Geist import — the bug, live and unambiguous, not inferred from CSS text.
- **After the fix**: `layout.tsx` confirmed clean (no `next/font` import); compiled `.next/static` output showed `.font-sans{font-family:Montserrat,sans-serif}` and `.font-heading{font-family:Roboto,sans-serif}` — real, independent literals, no Geist anywhere. Live Playwright check: `body`/`h1`/`html` all report `"Montserrat, sans-serif"`; a synthetic `.font-heading`-classed element independently reports `"Roboto, sans-serif"` (proving the alias is gone, not just the collision). With real font files (`cli/seeds/test-project-fonts/`, Roboto + Montserrat `.woff2`), `document.fonts` showed the pipeline's own Montserrat 400/600 entries `loaded` (the two weights the demo page actually uses) and the real `.woff2` files served with `200`/`font/woff2`.
- **`--update` verified twice**: once with the same fonts (font files newly added on an `--update` — confirms the pre-existing, ALREADY-disclosed font-face-on-update gap noted in the entry above is untouched/unchanged by this fix, not silently worsened or accidentally fixed as a side effect) and once with a real font CHANGE (Roboto/Montserrat → Lora/Nunito via a fresh `promote`/`generate` + `scaffold --update`) — confirmed both the `@theme inline` declaration and this pipeline's own override block picked up the new fonts correctly, idempotently, in one run.
- **No-`--fonts-dir` fallback (system-font stack) reverified working**: the primary before/after check above used no `--fonts-dir` at all — `document.fonts` correctly empty, no `@font-face` rules, clean `<html>` classlist, real fonts still named correctly in the compiled CSS for the browser to use if locally installed.
- `npx tsc --noEmit` clean. `qa:matrix --group=cli` (5/5) and `--group=core` (16/16, 2528 checks) both clean, no regressions.

**What was surprising going in**: the two files' near-identical surrounding code (same "append a later `:root` font override block" shape, same header-comment phrasing) suggested the same bug would reproduce the same way. It didn't — Next.js's `@theme inline` actually preserves `var()` indirection correctly (a real, structural difference from shadcn-vue worth knowing for anyone auditing a THIRD framework's shadcn integration later), and the real defect was an entirely different, previously unflagged interaction (`shadcn init` clobbering `nextjs.ts`'s own deliberate `next/font` stripping) that a "same bug, same fix" assumption would have missed even after patching the CSS. Worth remembering: matching code shape between two scaffold files doesn't mean matching root cause — verify each live.

---

## Checklist for whoever executes this

- [ ] Re-verify any version/API claim in this doc against real current source before relying on it.
- [ ] Confirm scope/order with the user at each fork (framework choice, component list, icon library default) rather than deciding unilaterally — matches this project's established working style.
- [ ] Write real decisions back into `pipeline-plan.md`/`contracts-and-seeds.md` as they're made, not just into this file.
- [ ] Test each new piece against a real run before calling it done, the same way every generator in this project was verified by actually running it and inspecting output (this is how the `--destructive-foreground` and RN Paper checker-regex bugs were both caught, earlier in this project).
